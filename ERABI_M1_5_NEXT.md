# ERABI — M1.5：追加学習の前に行う、入力経路と判断能力の切り分け

作成日：2026-09-18
対象：既存の erabi-local を実装している Codex / Antigravity
位置付け：AGENTS.md、KICKOFF.md、docs/ERABI_DESIGN.md を置き換えるものではなく、M0/M1実施報告を受けた次の作業指示。

## 最初に読む指示

既存の AGENTS.md、STATUS.md、該当する実装と実行結果を読み、このM1.5を実施してください。計画だけを提示して止めるのではなく、以下の範囲で確認・必要最小限の修正・比較実行・結果保存まで進めてください。

M2の追加学習、M3の校正・HTTP API化、ModernBERTへの置き換えは、今回はまだ実施しません。目的は「現在の失敗が、入力の渡し方、言語、タスクへの適性のどこから来ていそうか」を、少量の実験で明らかにすることです。原因が分からない場合は、未確定と記録してください。

既存環境・既存GPUジョブは保護してください。グローバル環境の変更、無断の依存関係一括更新、有料API利用、既存成果物の上書きは行いません。比較モデルは公開チェックポイントの取得とローカル推論のみとし、ダウンロード前に空き容量を確認してください。

## 1. 引き継ぐ観測と、まだ確定していないこと

添付の実装報告には、次の結果が記載されています。設計者側ではソースコード・生の実行成果物を受け取っておらず、再実行もしていません。実装担当側では実ファイルと照合してください。

| 項目 | 報告されている内容 |
|---|---|
| 環境 | Windows 11、Ryzen 7 5800X、RAM 96GB、RTX A4000 16GB、独立した .venv |
| Python / PyTorch | Python 3.12.10、PyTorch 2.6.0+cu124 |
| モデル | knowledgator/gliclass-multilang-mini |
| モデルrevision | 0bd888b6c3ef9fca5f0a9d407bddfbbc7623486b |
| CPUテスト | 17 passed |
| smoke評価 | 12問中4問正解、NLL 1.5182、Brier 0.8339 |
| 速度 | ウォーム推論 p50 28.02ms、p95 39.10ms、30回測定 |
| メモリ | ピーク562.02MBという報告。計測範囲・API・単位は実装を確認する |
| 未実施 | 追加学習、校正、HTTP API、完全オフライン確認、代替バックエンド |

同一の状況で「最安」と「最速」を切り替えても同じ誤答を高い確率で選ぶ、明示された条件に従えない、という失敗が報告されています。ただし、次の結論にはまだ飛躍があります。

- 「日本語の事前学習不足が原因と実証された」：原因は未確定。公開された20学習言語の一覧に日本語がないことは、基盤モデルを含む全学習履歴の日本語不在を証明しない。
- 「指示に全く追従できない」：この入力・この例での失敗であり、能力全般の証明ではない。
- 「問い合わせ分類は日本語でも高精度」：報告された少数例の成功であり、用途全体の精度ではない。
- 「12問中4問だからランダムと同じ」：候補数・問題分布を確認せずに比較しない。
- 「17テスト成功だから推論アダプターも正しい」：契約・数値処理のテストと、実モデルの入力整形・候補対応の検証は区別する。

STATUS.mdでは、過去の記録を黙って消すのではなく、追加の注記または更新欄で観測と推測を区別してください。

## 2. 最優先：現在の推論アダプターを小さく監査する

### 2.1 実際に何を渡しているかを見る

現在使っているライブラリ版・モデルrevision・トークナイザーrevisionを記録し、インストール済みのGLiClass実装を確認してください。公式のmainブランチとローカルの版が一致するとは仮定しません。確認のためだけにライブラリを更新しないでください。

公開モデルカードにはタスク指示を `prompt` に渡す用例があり、公開uni-encoder実装には、候補、区切り、prompt、本文を構成する処理があります。実装の分岐はconfigにも依存するので、特殊トークンや入力順を推測で手書きし直さないでください。[R1][R2][R3][R4]

代表的な2〜3問について、既存の入力スキーマの判断指示・本文・選択肢が、実際のモデル入力にどう対応しているかを確認してください。

確認するもの：

1. 最終入力へ、判断指示、本文、全候補の文章が欠落なく入っていること。
2. 判断指示を変えたとき、トークン列の対応部分も変わること。これは配線の検証であり、意味理解の証明ではない。
3. 候補の外部IDと、モデルへ渡した候補順、logitsの順の対応。
4. 特殊トークンの扱いが、当該モデル・トークナイザー・公式の整形方法と一致すること。
5. トークン上限を、本文だけでなく整形後の全入力で確認していること。上流のトークナイズ処理で暗黙の切り詰めが起こらないこと。
6. model.eval() と勾配を保持しない推論実行になっていること。

人工的な診断入力に限って、最終入力文字列または復号結果、token数、候補ID対応表を保存してください。個人情報を含む本番入力の常時ログ機構は作りません。

### 2.2 公式経路との照合

同じモデルrevision、トークナイザー、precision、入力整形、候補順で、公式の前処理とforwardに対応する経路とERABIアダプターを比較してください。代表例2〜3件で十分です。

公式APIが当該バージョンで全スコアを公開していればそれを利用し、公開していなければ、既存オブジェクトの前処理とforwardを使う短い診断処理で確認してください。公式ライブラリをforkしたり、大きなモック基盤を作ったりする必要はありません。

比較は、元のlogitsまたは同じ単一選択の後処理を施した全候補分布で行います。単一選択に対し、multi-labelのSigmoid出力を再正規化したものを、Softmaxと同一視しないでください。閾値で候補を落としてから正規化したり、確率に再度Softmaxをかけたりしていないことも確認します。

数値誤差の許容幅はdtypeと実行経路に合わせ、完全なビット一致を要求しません。差が大きい場合は、モデルの能力を論じる前に原因を調べてください。

### 2.3 入力形式に原因がありそうな場合

現在の形式と、モデルカード・インストール済み実装に沿った形式を、同じ問題で比較します。最初はこの2方式までです。何十種類ものプロンプト探索は行いません。

現在の形式が不適切と確認できた場合は、最小修正を施し、元の12問を再評価します。変更前の結果は残し、入力整形の版または差分を記録してください。

不一致がない場合は、「アダプター不良の証拠は見つからなかった」と記録します。単に間違えたからバグだと結論付けないでください。

## 3. 少量の対照実験：2モデル × 日本語・英語

### 3.1 最初の比較対象

- 現行：knowledgator/gliclass-multilang-mini
- 比較：knowledgator/gliclass-instruct-base-v1.0

instructという名前だけで性能向上や日本語対応を前提にしません。追加チェックポイントを一つ比較するだけです。共通化は必要箇所だけに限定し、汎用バックエンド基盤を作らないでください。

### 3.2 評価データ

最初は既存の日本語12問と、内容・正解・候補・判断条件を保った英語12問を使います。合計24問を2モデルで測定します。日本語の原文は保存し、既存のsmokeケースは上書きしません。

翻訳は外部有料APIなしで作成・点検してください。ルールの大小関係、否定、単位、境界値、候補の意味、引用内の偽命令を変えないでください。単なる英語化のつもりで、解きやすい追加説明や正解のヒントを足さないようにします。

日本語・英語・候補順変更版には同じscenario/group識別子を付け、別々の独立サンプルとは数えません。このデータは診断・開発用であり、今後の最終評価セットには使いません。

元データに正解の誤りや曖昧さが見つかった場合は、該当ID、理由、修正版を別記し、黙って採点結果を変えません。

### 3.3 診断項目

- 問い合わせ分類のような意図分類。
- 同じ状況で判断基準を切り替えるペア。
- 明示ルール、数値の境界、否定の理解。
- 情報不足の扱い。
- 引用された偽の指示と、外側の判断指示の区別。

「判断基準を変えたら答えが変わった」だけでは合格にしません。正解が異なるペアの両方に正解したかを記録します。候補を変えずに指示だけ変えるペアを含めてください。

候補順の比較は代表4問程度で、元の順と別の1順序を試せば十分です。ID・文章・正解を連動して並べ替え、モデルの位置への敏感さと実装上のID取り違えを分けて調べます。順序を変えても確率が完全一致する、というユニットテストは作りません。

24問では結論が不安定な場合に限り、新規の異なる状況を最大10組程度追加します。ペア問題を増やすことが目的であり、同じ文の言い換えを何百件にも増殖させません。

## 4. 記録する結果と、結果の読み方

### 4.1 最低限の記録

既存のevaluateと成果物形式を再利用し、runごとに以下を保存してください。

- model / tokenizer revision、関連ライブラリ版、precision、入力整形の版。
- 各問題の正解、予測、全候補確率、誤答例。
- 日本語・英語別の正解数/問題数、Accuracy、NLL、Brier。
- 判断基準切替ペアの両問正解数/ペア数。
- 候補順変更で正解候補の評価・予測がどう変わったか。

総合正答率だけでなく、どの問題で何が改善したかを見ます。少数例の差から統計的な優越性や汎用精度を主張しません。片方の入力形式を開発用データで選んだ場合、その選択に使ったことも明記してください。

### 4.2 読み方の目安（原因確定ではなく、次の仮説）

| 観測 | 次に調べる方向 |
|---|---|
| 公式整形に合わせると改善 | 入力経路や整形の不一致が影響した可能性 |
| 英語は解けるが日本語は難しい | 言語への適応が課題である可能性 |
| 両言語で同じルール問題に失敗 | 言語だけでなく、タスク適性・学習内容・問題形式も疑う |
| instructだけ改善 | チェックポイントと課題の適合性を調べる。理由は名前だけで確定しない |
| 順序変更で選択が大きく変わる | 位置への偏り、ラベル表現、ID対応を分けて確認 |
| すべて改善しない | その事実を記録。すぐに巨大データ生成やバックエンド総入れ替えへ進まない |

正の単一温度Tを使うsoftmax(z/T)は、候補の最大値の順位を変えません。したがって、誤った候補を一位にする問題を、温度スケーリングだけで解決しようとしないでください。確率校正と判断能力の改善は別の作業です。

## 5. テストは必要な差分だけ

既存の17件について、件数だけを理由に増やしたり削ったりしません。意味が重複するものは整理してもよいですが、今回のための大規模整理は不要です。

不足がある場合の追加候補は、原則この2種類です。

1. 判断指示と全候補が前処理へ渡されること、およびID対応が保持されることの小さな回帰テスト。
2. 公式経路とアダプターの一致を確認する、手動実行の小さな実モデル診断。

学習能力や日本語正答率は、通常のCPUユニットテストの合否条件にしません。モデルダウンロードやGPUを要する診断は、普段のpytest実行から分離してください。テストカバレッジやテスト件数の目標は設定しません。

## 6. 速度・オフライン確認は最小限に

既存の約28msという値は、まず当該ベンチ条件の報告値として維持します。測定コードにおいて、モデル読込、トークナイズ、GPU計算完了、CPUへの結果転送のどこまでが計時範囲かを確認し、notesに書いてください。必要な修正がある場合だけ同じ入力条件で再測定します。網羅的なベンチマーク基盤は作りません。

562.02MBについては、測定API・単位・対象範囲を明記し、学習時に必要なVRAMやGPUプロセス全体の占有量と混同しないでください。

モデルが既にキャッシュされていて容易なら、別プロセスでHFのオフライン設定とローカルファイル参照を使用した1問推論も実施してください。実際にネットワークを遮断していない場合は、「キャッシュ参照で推論できた」と「通信遮断下で確認した」を区別します。必要なキャッシュが不足しているなら、今回の主目的を止めず未実施として残します。

## 7. 作業範囲と完了時の報告

成果物の配置は既存構成を優先します。例えば runs/ 配下にM1.5の識別可能なrunを作り、短い比較メモと診断用JSONLを置けば十分です。新しいDB、UI、プラグイン機構、分散処理、APIサーバー、実験管理サービスは追加しません。

完了時には、STATUS.mdを更新し、次を報告してください。

1. アダプターに問題が見つかったか。見つかった場合は具体的な入力と差分。
2. 2モデル×2言語の結果。正解数/問題数を必ず併記。
3. 指示切替ペア・ルール・情報不足での代表的な成功と失敗。
4. 追加または修正したコードとテスト。変更が不要だった箇所もそのまま残す。
5. 実行コマンド、成果物のリポジトリ内パス、未実行項目。
6. 次の一手を一つに絞った提案と、その判断根拠。不明点も記録する。

その後に小規模追加学習を提案する場合は、「100件程度への過学習は学習配線の確認」「未学習の別シナリオでの改善が汎化の確認」と区別してください。診断に使用した12問や、その翻訳を学習させて正解させても、汎用判断能力が向上した証拠にはしません。

M1.5の成果は、必ずしも精度向上でなくて構いません。入力経路の妥当性と、失敗の傾向が以前より明確になれば有用な結果です。追加学習を開始すること自体を成功条件にしないでください。

## 参照情報・出所

[F1] ユーザー添付「貼り付けたマークダウン（1）(1).md」（実装報告）
- L154–176：実推論の出力と正規化の説明。
- L182–198：環境、モデルrevision、テスト報告。
- L204–223：12問の結果、速度・メモリ、代表的な失敗。
- L229–245：未実施項目と次の一手の提案。
上記は報告された情報であり、この補足文書の作成時に実装コードを監査したものではありません。

[R1] 開発元モデルカード：gliclass-multilang-mini。20言語の列挙、task prompt用例。確認日2026-09-18。
```text
https://huggingface.co/knowledgator/gliclass-multilang-mini
```

[R2] 開発元モデルカード：gliclass-instruct-base-v1.0。比較対象の提供、task prompt用例。確認日2026-09-18。
```text
https://huggingface.co/knowledgator/gliclass-instruct-base-v1.0
```

[R3] 開発元GLiClass README：Task Description Prompts。確認日2026-09-18。
```text
https://github.com/Knowledgator/GLiClass
```

[R4] 開発元pipeline.py：入力整形、単一選択の後処理。mainは可変なので実装ではローカル導入版と照合する。確認日2026-09-18。
```text
https://raw.githubusercontent.com/Knowledgator/GLiClass/main/gliclass/pipeline.py
```

この文書の実験順・件数上限・成果物案は、上記資料を参考にしたERABI向けの設計判断であり、開発元が規定する手順ではありません。
