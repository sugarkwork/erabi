# ERABI / 最初の実装指示

このファイルは、CodexまたはAntigravityへそのまま作業を依頼するための指示です。

## 今回の範囲

M0とM1だけを実装してください。設計書全体の機能を一度に作らず、まだ本格学習、校正、HTTPサーバー、GUI、課金データ生成を始めないでください。

まずAGENTS.md、STATUS.md、設計書の1～7・13～14・16～19節を確認してください。既存作業を調べ、設計書の前提と実機の違いを記録してください。

## 実行すること

1. OS、Python、仮想環境、PyTorch/CUDA、GPU名・枚数・空きVRAM、空きディスクを読み取り中心で確認し、環境情報を保存してください。必要ならプロジェクト専用環境を作ってください。既存環境の一括更新は禁止です。
2. 入力schemaと候補IDの保持、2～16候補、全入力512 tokens、無言の切り詰め禁止を実装してください。バイト上限、空候補、重複、内部マーカーの混入も小さく検証してください。
3. GLiClass multilang-miniの採用revisionとライセンスを確認してください。一問の実推論で全候補logitsを取得し、正しい確率分布を返してください。top1だけの結果から全分布を捏造しないでください。
4. JSONLを読んで予測とAccuracy/NLL/Brierを保存する最小コマンドを作ってください。examplesのデータは配線確認用であり、実用精度の証拠にしないでください。
5. 小さなCPUテストを実装して実行し、可能なら実モデルのGPUスモークを別実行してください。warm状態の一問の処理時間とVRAMを実測し、条件を記録してください。
6. 必要なREADMEと依存固定を整え、STATUS.mdへ実行結果・未実行項目・次の一手を残してください。

CLIは `python -m erabi doctor`、`predict`、`evaluate` のような小さなargparse構成で構いません。これらはまだ存在しない予定コマンドであり、実装後に実際の使い方をREADMEへ書いてください。

## 終了時に示すもの

実際に動いた入力と出力、環境とモデルrevision、検証したテスト、実測した項目、実行できなかった項目、日本語で特に失敗した代表例を示してください。最後に、既存モデルの追加学習へ進むか、別候補を測るべきかを理由付きで提案してください。

GPU・ダウンロード権限・ライセンス承諾等が不足する場合は、その阻害要因を明示してください。実行できるschema・数値・ファイル処理の実装を進め、mockの結果を実モデルの成功として報告しないでください。
