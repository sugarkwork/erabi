# ERABI — M3.2補足：個票比較とレビュー資料の完成

作成日：2026-09-18  
対象：既存 erabi-local を担当する Codex / Antigravity  
目的：次の追加学習の前に、報告を個々の予測結果から確認できる状態にする。

## 0. 今回はモデルも機能も増やさない

M3.2のラベル是正と比較再学習には、報告上、改善が見られる。本書は再学習をやり直させる指示ではない。既存の AGENTS.md、STATUS.md、該当コード・データ・比較結果を読み、集計の補足とレビュー用ZIPの完成だけを行う。

今回は再学習、新しいデータセット生成、校正、API変更、モデル交換、ライブラリ更新、追加の実験管理基盤を実施しない。既存の35テストを件数目標にして増減しない。作業は原則、保存済みJSON/JSONLの読取りと小さな集計で完結させる。

設計者が今回受け取ったのは実装報告テキストだけで、ローカルの review_bundle.zip、ソース全体、個票予測、モデル重みは未受領。以下の値はその報告に由来し、設計者の独立実測ではない。

## 1. 結果の解釈を限定する

報告値：

| 集合 | W_bad | W_fix |
|---|---:|---:|
| holdout_corrected | 178/200 | 191/200 |
| final_test_corrected | 96/200 | 144/200 |
| counterfactual_probe | 10/24 | 18/24 |
| transfer_probe | 23/32 | 23/32 |
| smoke_cases | 8/12 | 7/12 |

これらから「修正データで再学習した版では、この複数の評価集合で成績が改善した」と記録できる。一方、モデル内部の混乱の主因を解明した、未知タスクにも適用できる、マルチタスク学習以外では直らない、とは断定しない。

サーバー大容量はholdoutで12/12、counterfactualで6/6だが、final_test_correctedでは4/10だった。成功例だけを引用して、この種類の問題全般が解決したと書かない。

「手動定義」はコードで乱数生成していないという記述であり、人間が独立確認したこととは同じでない。実装エージェントが定義・自己確認した場合、human_verifiedという出所に変えない。

## 2. smoke12問の遷移を必ず個票で示す

同じ入力・正解であることを確認したうえで、B0 / W_bad / W_fix をIDで突き合わせる。新しい多数のケースを作らない。

今回の報告は、W_badが8/12、W_fixが7/12であり、smoke-06が誤答から正解へ回復したと述べている。この三つが同時に正しいなら、W_badで正解だった問題がW_fixで誤答になった件数は少なくとも2件ある。

```text
G = W_badで誤答、W_fixで正解になった件数
L = W_badで正解、W_fixで誤答になった件数
7 - 8 = G - L
smoke-06の回復により G >= 1
したがって L = G + 1 >= 2
```

次の列を持つ12行の表を保存する。

```text
id / 入力hash / 正解ID /
B0の予測・正誤 / W_badの予測・正誤 / W_fixの予測・正誤 /
W_bad→W_fixの遷移 / 各モデルのpmax・正解確率
```

W_bad→W_fixの新たな退行と、B0から継続している退行を区別する。集計がこの関係を満たさない場合は、データ版、予測ファイル、モデル参照先、集計処理のどこが違ったかを調べ、推測で説明を埋めない。

## 3. 正答数と確率品質を分けて報告する

transfer_probeでは正答数は23/32のままだが、報告値は以下のように変化している。

| 指標 | W_bad | W_fix |
|---|---:|---:|
| NLL | 1.1934 | 1.6564 |
| Brier | 0.4689 | 0.5261 |

「正答数は同じ」と「確率品質も維持した」は区別する。同じ23件に正解したのか、正誤が入れ替わったのかも個票から数える。新規の統計ライブラリは不要。

final_test_correctedではW_fixのNLL 1.9238はW_badの4.8992より低いが、B0の1.6698より高い。正答率と確率品質で比較結果が異なることを省かない。今回のために温度を調整して表を良く見せない。すべてT=1の結果を維持する。

過去の同じ旧モデル・旧ラベルでのholdout NLL/Brierと今回の再集計値が異なる場合、指標の計算版・precision・入力整形・checkpoint・予測ファイルを短く照合する。以前の記録を黙って上書きせず、変更理由が分からなければ未確認として残す。完全再現性の基盤を作る必要はない。

## 4. ペア集計と比較条件を補足する

正解を修正したholdoutには、正解IDが異なる80ペアと、正解IDが同じ20ペアがあると報告されている。B0 / W_bad / W_fixそれぞれについて、この内訳を保存済み正解から確認する。

- 全ペアの両問正解：一般的なペア成績。
- 正解が異なるペアの両問正解：指示や事実の変更に応じて答えを切り替えられたか。
- 正解が同じペアの両問正解：答えを変える必要がない場合に維持できたか。

全ペアの91/100を、そのまま「異なる答えへ正しく切り替えた91/100」と書かない。ペアIDに対応する二つの入力のどこが変わったかも、指示のみ・本文の事実・その他を分けられる既存情報があれば使う。新しい大きなスキーマは不要。

N問・2問1組のPペアについて、C=正答件数、B=両問正解ペアなら、片方だけ正解はC-2B、両方誤答はP-C+B。集計の整合確認に使えるが、独立試行数を水増しするためには使わない。

W_fixはEpoch 4、旧版は以前の報告ではEpoch 5が採用されている。これは各版のdevによる選択であれば不適切ではないが、「重み以外すべて同じ」「モデル内部の機序を解明」とは表現しない。checkpoint選択方法、seed、学習データhash、formatter版、評価precisionを保存済み記録から示す。

既に詳しく確認した final_test_corrected は、以後の改良では参照済みの診断集合として扱う。今回は新しい最終評価セットを作らない。

## 5. 生成器と修正スクリプトは実物を同梱する

今回の報告には正解をp == 0へ固定していた旨があるが、全コードを受け取っていない設計者は、数値・候補名・本文中の登場順に別の固定対応があるか判断できない。そうした不具合があると断定もしない。

その確認には build_m2_data.py、build_m3_data.py、build_m3_2_repair.py と実際の元データ・修正データが必要。今回は新しい大規模診断や生成器の作り直しを先行させず、実物をレビュー可能にする。

元の誤ラベル生成器を歴史的成果物として保存しているのか、現在も通常のデータ作成経路なのかをnotesに一行で明記する。誤った経路の新規実行を既定の手順として案内しない。大きな再設計や既存ファイルの削除はしない。

## 6. review_bundle.zipを不足なく作る

受領したログのinclude_patternsには、修正後データと比較集計はある一方、元データ、各モデルの個票予測、STATUS.md等が列挙されていない。実際のZIPを確認し、不足分だけ補う。ログではZIP作成後にSTATUS.mdを更新しているため、最新版を反映してから最後にZIPを作る。

最低限同梱するもの：

| 種類 | 内容 |
|---|---|
| 現状と再現条件 | 最新STATUS.md、README.md、pyproject.toml、既存の依存版・モデルrevision記録、今回のaudit/notes |
| コード | データ生成・是正・学習・推論・評価・比較スクリプトと、それらが参照する小さなローカルモジュール |
| 元データ | data/m2_1のtrain/dev/holdout、比較に用いた旧data/m3_1/final_test |
| 修正データ | data/m3_2_label_fixのtrain/dev/holdout_corrected/final_test_corrected、label_changes、manifest |
| 診断データ | counterfactual_probe、transfer_probe、audit_cases、smoke_cases |
| 個票と集計 | 3モデル×既存5評価集合のpredictionsとmetrics、再採点結果、train_summary、今回補足した遷移表 |

個票には、問題ID、候補ID対応、正解、予測、全候補確率が必要。既存で生logitsを保存しているなら含める。生logitsが未保存なら、その旨を記録し、丸め済み確率から高精度のlogitsやNLLを再現したことにしない。

原則、保存済み成果物だけを使う。必要な個票が本当にない場合のみ、現在の固定3モデルを同じ既存データで推論して再取得してよい。重み更新はしない。GPUが使えなければ不足を明記してZIPを完成させる。既存ジョブを停止しない。

重み、optimizer state、.venv、HFキャッシュ、APIキー、.env、環境変数ダンプ、個人の文章、無関係なフォルダは含めない。相対パスを保ち、プロジェクトフォルダ全体を無差別に圧縮しない。zipfile等の既存機能だけでよい。

保存先は既存の runs/m3_2_label_fix/review_bundle.zip を使ってよい。以前のZIPを残す必要がある場合のみ別名にする。ZIPを正常に読めること、必須ファイルが入っていることを確認し、同梱一覧と不足一覧を短く記録する。ローカルに作成したことを、外部へ送付済みと報告しない。

## 7. 終了条件と報告

既存テストは必要なら一度実行する。集計バグが発見された場合だけ、そのバグを防ぐ小さな確認を既存テストへ追加する。日本語の意味判断の点数をユニットテストへ大量移植しない。

完了時は、次の四点だけを報告する。

1. smokeで新たに誤答したIDと、3モデルの12行比較。
2. ペア内訳・確率品質・既存集計の相違について、訂正または補足した内容。
3. 確認できた実装・成果物と、まだ確認できない点。
4. 最新ZIPの実際の保存先、サイズ、不足ファイルの有無。

今回はマルチタスク学習へ自動的に進まない。その案は合理的な検証候補だが、必要性や成功を実証したものではない。モデル改善の次案はレビュー後に一つに絞る。

## 出所

ユーザー添付「貼り付けられたテキスト（1 点）(20260918-070819).txt」の実装報告を基礎とする。参照箇所：ZIP収録対象211–245行、ラベル変更274–289行、再採点293–308行、3モデル比較312–324行、原因解釈328–336行、成果物340–370行。これらは実装担当者の報告であり、本書作成者による実コードの監査結果ではない。

本書の件数計算・集計方法・補完範囲はERABI向けのレビュー判断。新しい学習済みモデルや実装成果物を含まない。
