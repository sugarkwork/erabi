# ERABI M3.4 — ZIPレビュー結果

作成日：2026-09-18  
対象：ユーザー添付M3.4報告および `review_bundle(2).zip`。旧モデルの個票は、前回添付された `review_bundle_followup.zip` から参照した。

## 結論

**通常学習の勾配蓄積は修正されている。保存された予測でも改善を確認した。次は対照損失の導入より先に、通常CEの学習予算を5→10エポックへ広げる一回の実験を推奨する。**

この推奨はレビュー担当者の設計判断。報告にある「独立サンプルCEでは指示より文脈に引きずられやすいので対照学習を導入する」という説明は、今回の実験で直接検証された因果関係ではない。

モデルの重みがZIPにないため、実モデルを再推論・再学習した結果ではない。保存logitsの再計算、コード差分、実際の学習ループを小さなモデルで通すCPU診断を行った。

## 1. 確認範囲

- 新ZIP：124,685 bytes、38ファイル。
- SHA256：`e42f8b8bdd8a983491dba869f2e21ef01a78dff30d92ae6c4e050dd59d993fca`。報告と一致。
- 4モデル×3集合、計976予測を再計算。新モデル244件、旧3モデル732件。
- 正解ID、候補IDと順序、argmax、保存is_correct、Softmax、NLL、Brierを対応させて確認。
- train/dev/eval_v2のファイルは前回ZIPとバイト単位で同じ。設定に記載されたデータhashとも対応する。
- 本番 `run_full_training` と `Trainer.compute_batch_loss` の関数本体をASTで取り出し、モデル・入力作成・optimizer・evaluateだけをテスト用に置き換えて実行。
- 新ZIPのみでのCPUテスト：31 passed、古いデータ欠落による4 failures、旧データ依存の1 skipped。校正テスト5件はGLiClass/Transformers未導入のため対象外。
- 別の作業コピーに、過去のユーザー添付ZIPから欠けた古いテストデータだけを補完し、36 passedを確認。残る5件を実行したとはしない。

初回4 failuresは、ZIPに旧データが含まれていないことが原因。本体の不具合の再発とは扱わない。レビュー用作業コピーだけを使い、ユーザーのリポジトリやアップロードZIPは変更していない。

詳細：`evidence/results/review_scope.json`、`recomputed_metrics.json`、`pytest_cpu*.txt`。

## 2. 勾配蓄積の修正

本番通常学習ループで、分母は更新ウィンドウ内の総サンプル数となっており、同じウィンドウの途中で変わらない。

```python
window_start_batch = (batch_idx // accum) * accum
window_start_sample = window_start_batch * batch_size
window_end_sample = min(window_start_sample + accum * batch_size, total_samples)
window_sample_count = window_end_sample - window_start_sample
loss_scaled = loss * (len(batch_records) / window_sample_count)
```

上は読みやすさのため変数名を短縮した要約。元コードの行番号付き抜粋は `evidence/source_evidence.md`。

小さなCPU診断は、16件、8件端数、15件奇数端数、23件の複数ウィンドウ、accum=1の5条件で実施。各optimizer.step()の直前に勾配を取得し、同じウィンドウを一括平均した参照と比較した。今回の合成例では各条件で一致した。

制約：固定の小モデル、dropoutなし、shuffleなし、勾配clipが作用しない大きな上限、optimizerは勾配記録のみで重み更新しない。GLiClassの学習曲線をCPUで再現したという意味ではない。

### テストと表現の小さな注意

添付の `verify_gradient_accumulation.py` と追加pytestは、同じ式をテスト側に書き直して検証している。本番学習ループを直接呼んだ検証ではない。複数ウィンドウの検証も、重み更新せず各ウィンドウの勾配を合算する方式。この制約を明記すべきだが、本レビューで本番ループの計算を別途確認できたため、さらに修正専用の実験を挟む必要はない。

報告の「全5条件で1e-8未満・完全一致」は、掲載値2.98e-8等と整合しない。添付JSONの許容値は1e-6で、正しくは「1e-6の許容範囲内で一致」。丸め誤差の説明の訂正であって、蓄積修正が失敗したということではない。

## 3. 保存予測から確認した成績

| eval_v2 / 200問 | B0 | W_fix | W_v2 | W_v2_gradfix |
|---|---:|---:|---:|---:|
| 正解数 | 89 | 106 | 117 | 162 |
| NLL | 2.2640 | 4.1315 | 0.8224 | 0.4761 |
| Brier | 0.9049 | 0.8503 | 0.5187 | 0.2755 |
| 異なる正解のペア /47 | 3 | 14 | 8 | 12 |
| 同じ正解のペア /53 | 16 | 13 | 28 | 52 |

各正解数・指標は報告の丸め桁で一致。個票の正誤や候補の対応に食い違いは見つからなかった。

transfer_probeは18→23→20→25/32、smokeは7→7→9→8/12。smoke-11の正解→誤答も一致。全体改善を「忘却なし」と読み替えない。

新モデルのtask_family別：

| 系統 | 正解数 |
|---|---:|
| goal_following | 69/100 |
| explicit_rule | 93/100 |
| goalの第一属性を尋ねる問 | 36/50 |
| goalの第二属性を尋ねる問 | 33/50 |

全体81%は有望だが、これは今回の合成問題・既知の診断集合に対する成績。任意の自然文判断で81%とはしない。

## 4. 切替の課題は確かに残るが、原因の断定はできない

異なる正解の47組では、新モデルは12組に両問正解、34組は片問のみ正解、1組は両問不正解。失敗35組はいずれも同じ候補を返していた。

内訳はgoal_following 3/31、複合条件3/7、境界値6/6、比較条件0/3。特に複数属性から選ぶgoal_followingが課題である。

ただし、**top-1が同じことは、質問変更が確率に全く影響していないこととは違う。** 35組とも分布には差があった。IDで対応させた全変動距離（1/2×各候補確率差の絶対値合計）は、約0.0030～0.2036、平均0.0735。これも文章理解そのものの証明ではなく、出力分布の観測である。

例：`eval_v2-gf-0004`。

```text
プランB：4300円・9日、プランC：2900円・2日、プランA：1700円・5日。
最安を尋ねる問：正解A、予測A。A=0.7570 / C=0.2238。
最速を尋ねる問：正解C、予測A。A=0.7170 / C=0.2627。
```

質問を変えると分布は変わるが、正解Cを一位へ押し上げるほど変わっていない。独立CEの限界、数値の対応付け、学習予算、表現への適応等のどれが主因かは、今回の比較だけでは分からない。

## 5. 次の実験を通常CEの10エポック比較にする理由

M3.4のdev履歴の終盤は次の通り。

| epoch | 正答率 | 全ペア両問正解率 | NLL |
|---|---:|---:|---:|
| 3 | 58% | 44% | 0.7064 |
| 4 | 75% | 54% | 0.6438 |
| 5 | 80% | 62% | 0.3778 |

上限に到達した時点でも改善している。5エポックは過去の設計で置いた予算であり、収束や十分な学習を示す数字ではない。まず同じ通常CEのまま学習予算を広げる方が、新しい損失・バッチ構成を加えるより変更が少ない。

ただし、これで改善すると予言するものではない。過学習や退行もあり得るので、一回・最大10エポックまでに固定し、train/devで切替が学べているかも記録する。

新しい損失を導入するときも、ペアCEの平均と独自のcontrastive lossは分ける。指示が異なるだけで分布を離すと、同じ正解の53組に誤った目的を課すことになる。

## 6. 次回も残る制約

- eval_v2は既に何度も設計判断に用いた診断集合。未使用最終試験とは呼ばない。
- 前回見つかった11group22問の数値・条件構造の共有は、今回は直していない。178問の補助集計が良くても、この制約が完全に消えたわけではない。
- 重み・optimizer stateが未添付のため、実際に使われたB0の同一性や学習再現性を本レビューから独立証明できない。データhashとコード差分は確認できた。
- 現モデルは未校正、APIのreview固定を維持する。温度調整でtop-1の切替が修復されるとは考えない。

これらを記録しつつ、同条件の学習予算比較を進めてよい。今回はデータの再構成、モデル交換、監査基盤の拡張へ戻らない。

## 7. 同梱物

- `ERABI_M3_5_CE_BUDGET_NEXT.md`：実装エージェントへ渡す次の指示。
- `evidence/recompute_m3_4.py`：保存logitsの再集計。標準ライブラリのみ。
- `evidence/verify_uploaded_loop.py`：実ソースの関数を小さなCPUモデルで通す診断。PyTorchが必要。
- `evidence/source_evidence.md`：今回確認したソース箇所。
- `evidence/results/`：再集計、実行範囲、勾配検証、テストログ、各モデルのペア個票。

コード本体やモデル重みを変更した成果物ではなく、レビューと次の実験設計である。

## 技術背景の外部出典

PyTorch公式CrossEntropyLoss：クラスindexを正解に取り、各サンプルの損失をmean等で集約する。外部資料はERABIの10エポックの有効性を裏付けたものではなく、損失の意味の確認に用いた。
```text
https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html
```
