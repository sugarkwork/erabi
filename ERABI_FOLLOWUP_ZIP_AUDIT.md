# ERABI M3.3追補ZIP — コードと保存予測のレビュー

作成日：2026-09-18

## 結論

追補報告の集計は保存予測と一致する。しかし、M3.3で通常学習の勾配蓄積に新しい不具合が入っている。**ペア学習を追加する前に、この計算だけを修正して同条件の学習を1回行う**ことを提案する。

不具合の存在・CPU小例での影響は確認した。これが実モデルの成績低下をどれだけ説明するか、修正で何点上がるかは未検証であり、予断を置かない。

## 1. 入力と実施範囲

| ZIP | bytes | ファイル数 | SHA256 |
|---|---:|---:|---|
| review_bundle_followup.zip | 183065 | 44 | 4179fc2569b3d42f65a7b9bcdc5a9fbc8121fdbd0cff3618eebf3352b0491ed0 |
| review_bundle(1).zip | 144288 | 40 | 269c0aa6fb63cef90d4bb55e3caf3c8f1c626858e700f3279041eb508c1f5508 |

安全なパスで別ディレクトリへ展開し、ZIP間の内容差を照合した。追補版で変わったのはSTATUSと、追加の監査スクリプト・追補成果物。学習・推論・生成器などの共通コードは同じだった。

実施：コード読解、保存予測9ファイル732件の独立再計算、入力指紋の照合、明示ルールの構造状態比較、実ループを使うCPU勾配診断、既存CPUテストの一部。

未実施：本物のGLiClassの推論、再学習、GPU測定、モデル重みの照合、モデルの注意機構・内部特徴の分析。ZIPにモデル重みは含まれていない。

## 2. 保存された成績は一致

| 集合 | B0 | W_fix | W_v2 |
|---|---:|---:|---:|
| eval_v2 200問 | 89 | 106 | 117 |
| smoke 12問 | 7 | 7 | 9 |
| transfer 32問 | 18 | 23 | 20 |

正答数だけでなく、raw_logitsからのsoftmax、候補IDとの対応、正解、NLL、Brierも再計算した。報告値の丸め桁で一致した。

正解が異なる47組のW_v2は、両問正解8組、同じ予測34組、異なる予測13組。この39組の失敗のうち34組が未切替という分解も一致する。

「アイテムありの語彙バイアスが原因」という説明は仮説に留める。両方healを出した観測は確認できるが、内部の判断理由を証明したものではない。

## 3. 最優先：勾配蓄積の除数

対象：`src/erabi/train.py`、通常学習 `run_full_training` 内の約399–408行。

```python
window_offset = batch_idx % trainer.gradient_accumulation_steps
remaining_in_window = trainer.gradient_accumulation_steps - window_offset
remaining_in_epoch = total_micro_batches - batch_idx
current_window_size = min(remaining_in_window, remaining_in_epoch)
loss_scaled = loss / current_window_size
```

8回分を平均すべきところ、残り回数で割るため、除数が8,7,6,5,4,3,2,1になる。

```text
期待：L1/8 + L2/8 + ... + L8/8
実装：L1/8 + L2/7 + ... + L8/1
```

後のミニバッチほど強く学習する。各ミニバッチが同じ2件なら、8番目の係数は1番目の8倍。通常ウィンドウ全体に発生するので端数のみの問題ではない。

### CPUでの再現

アップロードされた実際の `run_full_training` と `Trainer.compute_batch_loss` をASTで取り出し、その関数本体を変更せず、小さな決定的CPUモデル・記録用optimizerで実行した。依存モデルの読込、データシャッフル、重み更新、評価は小さな代替にして、除数計算・損失計算・backward・stepの境界を検査した。

16件の合成例（14件はx=2,target=1、2件はx=4,target=0、logits=[w*x,0]、w=0）：

| 計算 | 勾配 |
|---|---:|
| 16件を一括した平均CE | +0.625 |
| 現行ループ、2件×8回 | -0.28214283287525177 |
| 修正案、2件×8回 | +0.625 |

この例では向きまで変わる。実GLiClassが同様の向きになっていたことを示す実測ではない。

8件の部分ウィンドウ、15件で最終1件のミニバッチ、23件のフル＋端数、蓄積1回も比較し、修正案は全5条件で一括平均の勾配と一致した。CPU PyTorch 2.10.0、比較許容誤差1e-6。参考差分は監査した旧ソースに対して `git apply --check` が成功した。ユーザーのリポジトリへは適用していない。

正しい重みは `ミニバッチ実件数 / 更新ウィンドウ総実件数`。詳しくは次の指示書と `gradient_accumulation_fix.patch` を参照。

M3.2とのdiffで、この不具合はM3.3の端数対応追加箇所にあると確認した。以前の版は一律accumで割っており、こちらは端数ウィンドウの大きさを正しく扱えていなかった。単に元へ戻すのではなく、通常と端数を両方直す。

## 4. ペア学習の提案について

単純なペア平均 `L=(CE1+CE2)/2` は、同じ重み・同じサンプルなら通常の行単位CEの平均と同じ目的関数。ペアを同じバッチに入れることで更新のばらつき等は変わり得るが、そのことと新しい対照目的を加えることは別。

また、質問が異なるという理由だけで予測を分離すると、正解が同じ53組を壊す可能性がある。必要なのは「質問が違えば違う答え」ではなく「各問の意味に応じた正解」。提案が有効かどうかの実験はまだ行われていない。

今回は先に通常学習の計算を正し、ペア損失・ペアバッチ・重み付けの追加を同時に行わない。

## 5. 完全一致入力と、同一構造状態は別

順序非依存の候補文と正規化した本文・質問での照合では、train/dev/eval間の完全一致は0件だった。これは追補報告と一致する。

しかし、明示ルールで数値と条件が同じで、表現だけがseenからnovelへ変わったものが残る。

| 対応 | 共有する状態数 |
|---|---:|
| train ↔ eval | 9（composite_logic 8、boundary 1） |
| dev ↔ eval | 2（composite_logic 2） |
| 合計 | 11組22問。eval側は全てnovel表現 |

composite_logicのeval10組は、同一のHP・閾値・アイテム状態がtrainかdevに存在する。この評価は主にその条件の言い換えへの転用を測っており、完全に新しい状況のテストとは言えない。

上記22問を除いた178問では、B0 74問、W_fix 88問、W_v2 102問正解。改善そのものは残る。正解が異なる39組での両問正解は2・10・7組。

今回の勾配修正比較ではデータを変えず、この制約を明示する。データを直すことと勾配を直すことを再び同時に行うより、まず固定条件で修正を比べる。178問も、すでに結果を見て開発している集合なので独立な最終評価と呼ばない。

## 6. テストの解釈

同梱テストでは、例えば最大容量の独立期待値確認が `assert "a" == "a"` となっており、生成器の実処理を通っていない。`test_argmax_pre_rounding_edge_case` もテスト内で書き直したループのみを確認している。

テスト件数を増やすより、実際の本番処理を呼び出す少数の確認へ置き換える方が重要。今回の回帰検証は「勾配の蓄積が一括平均と一致する」という1責務を中心にする。

レビュー環境の既存pytest実行結果：30 passed、4 failed、1 skipped。4 failedは旧M2.1/M3.2のデータが今回の小さなZIPに含まれず、前提ファイルが見つからないため。1 skippedも旧データ不在。校正5件はGLiClass/Transformersがレビュー環境にないため最初から除外した。この結果を、ユーザー環境の40 passedの反証やアプリの新しい不具合とは扱わない。

## 7. 次の作業

`ERABI_M3_4_TRAINING_FIX_NEXT.md` に実行範囲をまとめた。内容は、実ループの勾配検証、最小修正、同一データ・B0・設定から1回の学習、既存3集合での比較まで。

今回の修正で実精度が改善する保証はない。改善しなくても、計算を正した上で残る課題を分けられる。再学習をせず新しいレポートだけを重ねる段階ではなく、今回の範囲内で実際の修正と比較まで進める。

## 8. 同梱根拠

- `bundle_summary.json`：入力ZIPのサイズ・SHA256。
- `source_excerpt.md`：元コードの行番号付き抜粋。
- `training_diff_from_m3_2.patch`：前のアップロードZIPとの学習コード差分。
- `recompute_followup.py`：保存予測の再計算。
- `results/recomputed_summary.json`：732件、指標、完全一致・構造状態照合。
- `results/semantic_state_examples.jsonl`：同一状態の具体的な元入力。
- `results/semantic_overlap_sensitivity.json`：22問を除いた補助集計。
- `repro_accumulation.py`：実ループを利用するCPU小モデル診断。
- `results/gradient_probe.json`：5条件の勾配比較と実行範囲。
- `results/gradient_accumulation_fix.patch`：最小修正の参考パッチ。
- `results/pytest_cpu.txt`：既存テストの成功・前提不足の記録。

## 技術背景の出所

PyTorch公式の勾配蓄積例は、等サイズミニバッチのlossを固定の蓄積回数で割る。CrossEntropyLossのmean定義を用いると、端数を含むERABIのケースではサンプル実件数による重み付けが必要になる。これは本ZIPの性能を保証する資料ではない。

```text
https://docs.pytorch.org/docs/2.14/notes/amp_examples.html#gradient-accumulation
https://docs.pytorch.org/docs/2.14/generated/torch.nn.CrossEntropyLoss.html
```

コード上の問題はアップロードされた実ファイルとCPU再現に基づく。次の実験範囲はレビュー担当による提案であり、ユーザー報告に既に実行済みの手順ではない。
