# ERABI — M3.6 ZIPレビュー：校正結果と生成・読込の整合性

作成日：2026-09-18  
対象：ユーザー提供 `review_bundle(3).zip` とM3.6完了報告  
ZIP：184,828 bytes / 52 files  
SHA256：`154973f51914402b9309c0803f1e88d72c371d8c9e4d4b4d2946bd0b2a287162`

## 結論

**保存logitsから、単一温度による校正の改善を再現できた。重み・データ・温度を作り直す必要はない。**

ただし、校正作成スクリプトが出す `status="applied_scoped"` と、APIが適用する `status="applied"` が一致していない。今回同梱された校正ファイルは `applied` に変更されており、そのファイルを使ったAPI成功報告とは矛盾しない。一方、スクリプトで再生成したファイルを使うと、既定設定では未校正T=1へ戻る経路がある。

次はマルチタスク再学習ではなく、**校正成果物の生成・照合・APIへの受け渡しを、手編集なしで再現可能にする**最小修正を行う。データや学習方法を変更する実験は混ぜない。

## 1. 今回実際に確認した範囲

- ZIPのサイズ・SHA256・52ファイルの内容を確認。重みや仮想環境は含まれていない。
- M3.6のcalibration/fresh_eval各200件と、M3.5のeval_v2 200件・smoke12件・transfer32件、**計644件**の保存logitsを入力データのID・候補ID順・正解と照合。
- 保存された正誤フラグに頼らず、raw logitsのargmaxと正解IDから正答数を再計算。FP64でlog-sum-exp、NLL、Brier、信頼度帯、閾値別件数、ペア成績を計算。
- calibrationの200件のみで単一温度を独立に最適化。fresh_evalやtransferを温度推定に使っていない。
- 新規400件の自然文を、生成器の正解決定関数を呼ばない別処理で読み、既知の合成文法について正解ラベルを照合。
- 新規2集合と既存集合の入力一致、および解析可能な同型シナリオの数値状態の重複を確認。
- 実際のAPI・校正ローダーを、小さなfake scorerやダミーファイルとともにCPU上で動かして境界の不具合を再現。
- 既存CPUテストの実行可能な部分を実行。

**実施していないこと：** GLiClassの実モデル再推論、実モデルのHTTP起動、GPU学習、同梱されていない重みのSHA256の独立検証。依存ライブラリを追加インストールしていない。保存logitsが指定モデルから実際に生成されたという完全な出所保証は、このZIPだけでは得られない。

レビュー環境はPython 3.13.5 / torch 2.10.0+cpu。ユーザーのWindows実行環境と同一ではない。詳細は `ERABI_M3_6_REVIEW/results/environment_and_scope.json`。

## 2. 校正の数値結果は一致した

| 集合 | 正解数（温度で不変） | NLL T=1 → 校正後 | Brier T=1 → 校正後 |
|---|---:|---:|---:|
| calibration | 189/200 | 0.343612 → 0.157234 | 0.100898 → 0.085393 |
| fresh_eval | 188/200 | 0.348948 → 0.167539 | 0.117950 → 0.095721 |
| 既知eval_v2 | 195/200 | 0.153800 → 0.088014 | 0.048435 → 0.046498 |
| smoke | 9/12 | 2.123863 → 0.941297 | 0.490176 → 0.424523 |
| transfer | 25/32 | 1.623907 → 0.770522 | 0.475397 → 0.446960 |

保存された推定値は `T=3.109686582278409`。独立計算では `T=3.1096968871451347`。最適化の許容幅の差であり、同じ小数4桁の約3.1097になる。独立最適値でのcalibration NLLは約0.1572337202431534。

配信用artifactには `T=3.1097` と丸めて保存されている。推定値と配信用丸め値の双方で再計算し、この差が本結論を変えないことを確認した。次の工程で温度を再探索する必要はない。

fresh_evalのペアは88/100組両問正解、異なる正解41/51組、同じ正解47/49組。報告と一致する。

記録：`ERABI_M3_6_REVIEW/results/recomputed.json`。

## 3. 実装不整合A：作成した校正ファイルがそのままAPIへ適用されない

### 原因

`scripts/run_m3_6_calibration.py` L520–538は採用時に以下を保存する。

```python
adoption_status = "applied_scoped"
# ...
calibration_artifact = {"status": adoption_status, ...}
```

しかし `src/erabi/api.py` L57–75は `status == "applied"` の場合だけ温度を適用する。それ以外は、require_calibrationがfalseなら警告してT=1へ、trueなら起動失敗となる。

### 確認結果

同梱物の状態は次のように異なる。

- `summary_report.json`：採用status `applied_scoped`
- 現在の `calibration.json`：status `applied`
- 提供された作業ログには、calibration.jsonを編集した記録がある。

**現在のファイルでAPIがT=3.1097を使えたという報告を否定するものではない。** 現ファイルの再起動ではなく、現スクリプトで再生成したファイルを使った場合の再現性の問題である。

出力statusをソースと同じ `applied_scoped` にした一時ファイルを実APIへ渡すCPU診断では、`GET /health` が次を返した。

```json
{
  "temperature": 1.0,
  "calibration": {"status": "none", "artifact_id": null}
}
```

`require_calibration=True` では、`Calibration artifact status is 'applied_scoped', but calibration was required.` で停止した。

### 最小修正

既存runtime契約 `applied` / `none` を維持し、限定用途という判断は `scope` / `adoption.decision` へ分ける。未知のstatusを、明示した校正の代わりに黙って未校正として扱わない。未校正で動かす選択は、明示した別の起動として残す。

テストは、手で正しく書いたJSONをローダーへ渡すだけでなく、**本物の書き出し処理→読込→API**を一度つなぐ。

## 4. 校正とモデル・キャッシュの結び付けにも不足がある

### 4.1 存在しないローカルモデルでhash照合が省略される

`load_and_verify_calibration` は `if os.path.isdir(model_id):` の中でのみ対象ファイルのhashを照合し、その条件を満たさなくてもstatus=appliedを返す。

実ローダーへ `model_id="not-a-real-directory/or/model"` を渡す診断で、モデルhashを一つも照合せず、T=3.1097/status=appliedが返ることを確認した。実エンジンがその後のモデルロードで失敗する可能性とは別に、「照合済みの校正」として受け入れるローダーの条件が不足している。

今回はローカル版なので、校正適用時は存在するローカルcheckpointを必須にすれば十分。リモートモデル解決サービスを追加する必要はない。

### 4.2 contractの内容を読んでいるだけで照合していない

ダミーのローカルcheckpointと、実ファイルに合うhashを使い、artifactへ意図的に異なる `schema_version`、`max_tokens`、`precision`、`formatter_version` を設定しても、実ローダーはappliedを返した。

現artifactにはformatter版も記録されていない。数値上の校正が正しいことと、別の入力整形・精度設定へ誤適用できないことは別である。

現実装の入力整形版・上限・実際のforward精度等を小さな共通設定として渡して照合する。クラス階層やモデルregistryは不要。重み・tokenizer/configの実ファイルhash照合は引き続き使用する。

### 4.3 保存logitsをファイルの存在だけで再利用する

`run_m3_6_calibration.py` L387–427は、現在のcheckpoint hashを取得した後、`raw_logits_*.jsonl` があれば出所照合なしで再利用する。その後のartifactには現在のcheckpointとデータのhashが書かれる。

**コード上の将来リスク：** モデルやデータを変えて同じ出力先を再利用すると、古いlogitsへ新しいhashを付ける可能性がある。今回の保存logitsとデータのID・正解・候補は一致しており、「実際に別モデルのlogitsだった」とは確認していない。

小さなsidecarに、モデル関連ファイルhash・入力データhash・formatter/precision・候補対応を記録し、再利用時に照合するだけでよい。不一致なら止める。DBやキャッシュサーバーを作らない。

過去のlogitsへ、生成時に記録していなかった情報を後付けして「当時検証済み」としない。旧成果物はlegacyとして残し、照合可能な既存記録か、固定条件での必要最小限の再推論によって移行を検証する。温度や重みを学習し直すこととは別作業である。

## 5. 新規データに重複は見つからなかったが、GFの重複検査キーが不一致

独立チェックでは、新calibration/fresh_eval各200件について次を確認した。

- 互い、および既存train/dev/eval/smoke/transferとの正規化入力一致は0件。候補順をソートして比べても0件。
- 新400件、既存train600/dev100/eval200は既知合成文法の意味状態を全行抽出でき、その比較では重複0件。
- smokeは4/12のみ意味状態を解析、transferは0/32。これらに関する「意味状態重複0」は未解析部分の独立性を保証しない。完全一致入力の検査は全件実施した。
- 新400件の正解を、表示された自然文と候補から独立に確認し、不一致0件。対象は今回の限定的な文法であり、任意自然文のgold認定ではない。

ただし `build_m3_6_data.py` には、生成時と既存データ読込時でGFのキーの形が違う。

```text
生成時：("gf", domain, sorted((value1, value2), ...))
読込時：("gf", flat_numbers_in_context_order)
```

実際の生成関数で作った1組をJSONLへ書き、実際の既存状態抽出関数で読み直すと、自分自身の状態に一致しなかった。

**今回の漏洩を見つけたという意味ではない。** 独立再計算では重複はなかった。しかし次の生成で同じシナリオを除くための検査は成立していないので、キーを統一し、同じ保存済みデータで再確認する。今回は新規データを作り直さない。

## 6. 校正の範囲は「ルールが明示されている問題全般」ではない

fresh_evalのcalibrated pmax>=0.9は168問、正解164問（97.6%）、平均pmax約97.9%。この集合では良い対応である。しかし、同じ温度を適用したtransferのpmax>=0.9は9問中7問正解で、2問誤答した。件数も問題分布も違うため、一般精度の直接比較には使わないが、保証の範囲を広げられないことは明らかである。

さらにsmoke-06は、必要なルールがすべて本文・質問に書かれていても、校正後のpmaxが約0.996502で誤答する。

```text
状況：HPは15。回復アイテムあり。安全地帯へ移動中。
ルール：移動中なら移動を継続。それ以外でHP<20かつ薬があれば回復。
正解：移動を継続する
予測：回復する（校正後 約99.65%）
```

これは「情報が足りない問題」ではない。現在の合成データで主に扱った条件形式から外れた、例外の優先順位を含む問題である。

したがって、評価で支持された範囲は、生成テンプレート・語彙・条件形式・数値分布・2～3候補などを含めた限定的な範囲として記録する。入力検証が2～16候補を受け入れることは、4～16候補でも校正済みであることを意味しない。

fresh_evalのpmax>=0.95は152問、観測誤答0件だが、有限サンプル・ペア相関があり、真の誤り率0の証明ではない。自動acceptは有効化しない。`applied` は温度を適用した事実だけで、入力が対象分布内という保証ではない。

また今回の新データはERの内訳や数値範囲が以前から変更されている。calibrationとfresh_evalの配分は同じだが、旧eval_v2と同一分布の再試験として94%と97.5%を単純比較しない。

## 7. 報告・小集計の軽微な修正

- smokeの正解維持は**7件**。報告は「9問」と書きながらIDを7個列挙している。旧ZIPの保存予測とも照合し、維持7・回復2・退行1・両方誤答2、最終正解9件を確認した。
- 最適温度は探索範囲0.05～20の**内部で、境界に到達していない**。「境界外で収束」は言い換える。
- 確信度帯の平均が近いことを、完全校正や未知用途の保証と表現しない。
- `evaluate_dataset_at_temperatures` は一部で作ったpair_statsをreturnで使わず作り直している。ペアにならないsmokeに無意味なペア成績を返さない。
- 候補数別の `else` を3択扱いにするため、smokeに含まれる4択が3択欄へ入る。2択・3択・その他を正しく分けるか、対象外をnullと件数で示す。今回のcalibration/fresh_evalは2～3択だけなので、主要値を覆す誤りではない。

これらのためにモデルやデータを再生成しない。既存ログ・集計の小修正で十分。

## 8. テストと再現資料

実行した既存テスト：

```bash
PYTHONPATH=src HF_HUB_OFFLINE=1 python -m pytest \
  tests/test_api.py tests/test_contracts.py tests/test_m3_3.py \
  tests/test_scores.py tests/test_train.py -q
```

結果：**31 passed / 1 skipped**。スキップはこのZIPにない旧データに依存。別の校正テスト5件は未導入GLiClass/Transformersに依存するため、repairテスト4件は旧データがZIPにないため、このコマンドの対象外にした。41件全部の再実行成功を主張しない。

追加診断は、実ローダー・APIを通すがモデルはfake scorer。ローカルhashテストは実hash helperを使ったダミーファイル。学習済みモデルの意味的能力をCPUのfakeで確認したという意味ではない。

同梱の再現コマンド：

```bash
python ERABI_M3_6_REVIEW/audit_saved_results.py /path/to/extracted/review_bundle results
python ERABI_M3_6_REVIEW/probe_contracts.py /path/to/extracted/review_bundle results/contract_probes.json
```

レビュー対象と同じ版が必要。前者はnumpy/scipy、後者は既存APIのCPU依存を使う。学習や外部ダウンロードを行わない。後者は不具合を再現するレビュー用で、修正後にも同じ失敗を要求するユニットテストではない。

## 9. 次の工程

`ERABI_M3_7_CALIBRATION_HANDOFF_NEXT.md` に範囲を限定した。

固定するものはW_v2_ce10、入力データ、今回の温度。直すのはartifactの状態整合、モデル/入力条件/cacheの照合、GF状態キーの往復、報告の小集計。既存の推論・校正・テストを再利用する。

完了後は、今回のモデルを「合成ルール用途の実験用基準版」としていったん保全する。次の能力拡張は別の実験で一用途ずつ行う。今回の否定・例外問題がNLIや問い合わせデータを少し混ぜれば必ず解決する、という根拠はまだない。
