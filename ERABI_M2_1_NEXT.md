# ERABI — M2.1：小規模追加学習で「暗記」と「未学習問題への改善」を分ける

作成日：2026-09-18  
対象：既存の `erabi-local` を実装する Codex / Antigravity  
位置付け：M1.5実施報告を受けた追加指示。総合設計書、AGENTS.md、既存API契約を置き換えない。

## 0. 今回の作業範囲

既存の `AGENTS.md`、`STATUS.md`、本書、学習に関係する総合設計書の節を読み、**M2の最初の小規模実験だけ**を実装・実行してください。

今回は `knowledgator/gliclass-instruct-base-v1.0` を**実験用の暫定ベース**とします。最終採用・汎用日本語性能・Jev同等性能が確定したわけではありません。

進める順番は次のとおりです。

1. M1.5の実ファイルと設定を確認し、学習前の状態を固定する。
2. 正解をプログラムで確定できる少量の日本語データを作り、グループ単位で分割する。
3. 64件以下への過学習で、学習・保存・再読込の経路を確認する。
4. 元の未追加学習チェックポイントから、600件を目安とする追加学習を行う。
5. 学習に使わない別の問題で学習前後を比較し、結果と限界を報告する。

計画書の作成だけで終了せず、この範囲の実装・実行・保存まで進めてください。環境上の承認ゲートや安全制約は尊重し、迂回しません。課金、外部送信、破壊的変更、範囲拡大が必要になった場合は勝手に実行しないでください。

**今回は行わないこと：** 大量データ生成、教師API、ソフトラベル蒸留、強化学習、ModernBERTへの交換、第三の比較モデル、校正、HTTP API、UI、量子化、分散学習、独自プラグイン基盤。

元のM1.5には追加学習を保留する指示がありますが、本書では上記に限定して追加学習へ進みます。総合設計書のM2全体を一度に完成させる指示ではありません。

---

## 1. 引き継ぐ報告値と、解釈の範囲

以下はユーザーが貼り付けたM1.5報告の記載です。本書作成側はローカルのコード・予測JSONLを直接監査していません。実装担当は実成果物と照合し、相違があれば記録してください。

| モデル・条件 | 正解数 | NLL | Brier | p50（報告値） |
|---|---:|---:|---:|---:|
| multilang-mini / JA | 5/12 | 1.4937 | 0.8710 | 23.02 ms |
| multilang-mini / EN | 6/12 | 1.4033 | 0.7719 | 35.75 ms |
| instruct-base / JA | 7/12 | 1.3799 | 0.6261 | 29.52 ms |
| instruct-base / EN | 7/12 | 1.8890 | 0.6677 | 32.73 ms |
| multilang-mini / JA候補順反転 | 2/4 | 2.4174 | 0.9395 | 143.13 ms |
| instruct-base / JA候補順反転 | 1/4 | 4.5259 | 1.2518 | 129.47 ms |

報告上のその他の観測：

- 公式推論経路との照合では、入力欠落・ID対応不良の証拠は見つからなかった。
- `question` を `prompt`、`context` を `text` に渡す改修後、miniの日本語結果は4/12から5/12へ変化した。
- 最安・最速の指示切替ペアは、いずれのモデル・言語でも両問正解には至らなかった。
- instructは候補順を変えた4問中2問で選択が変わった。miniは4問とも同じ選択だった。
- CPUユニットテスト17件が成功した。HFオフライン設定で推論できたと報告されている。

### 解釈を修正・限定する点

- 7/12対5/12は、この診断セット内での2問差。「明確な汎用的優越」「日本語能力を十分に実証」とは書かない。
- 英語ではinstructの正解数は増えたが、NLLはminiより大きい。確率に関する指標が全面的に改善したわけではない。
- 順序変更で選択が変わったことは「順序への感度」の観測。「先頭を好む」と断定するには、選択位置の対応等が不足している。
- 4/4の順序一致も、正確さや全般的な順序不変性の証明ではない。
- 失敗が残ることから「追加学習が唯一必要な解法」とは結論しない。今回は追加学習の効果を実験する。
- 24入力（12問のJA/EN）を2モデルで評価した48推論と、反転4入力×2モデルの8推論は、独立な56問題ではない。同じ状況の翻訳・指示違い・順序違いを独立標本として水増ししない。
- 4問の反転セットと12問の通常セットの速度は、入力長・ウォームアップ等が揃っていない可能性がある。表だけで順序反転が遅延を生むとは結論しない。

過去のSTATUSを消さず、今回の更新欄で観測と仮説を分けてください。

---

## 2. 最小限の前準備

### 2.1 既存のものを維持する

既存の `.venv` とモデルキャッシュを利用します。Python / PyTorch / Transformers / GLiClassを確認のためだけに更新しません。別のGPUジョブを停止せず、GPUの空きメモリと保存先の空き容量を確認します。

利用モデルのrevision、トークナイザーrevision、ライブラリ版、入力整形の版を記録してください。instructのrevisionをminiの値から流用しないでください。

元のモデルキャッシュ、M1/M1.5の `runs/`、既存の12問は上書きしません。追加学習モデルを既定の推論モデルへ自動昇格させる変更も不要です。

### 2.2 入力を再設計しない

M1.5で監査した入力形式を学習でも利用します。学習用の1～2例について、候補順の拡張を無効にした状態で、**学習と推論の入力トークン列・attention mask・候補対応**を照合してください。

これは学習経路を新設する差分の確認です。M1.5を全面的にやり直す必要はありません。

公開GLiClassには `prompt`、`prompt_first`、候補並べ替え、augmentation等の分岐があります。公式の現在のmainとインストール済み版を同一視せず、実際の関数シグネチャとconfigを確認します。[R1][R2]

**注意：** 公開 `train.py` をそのまま流用すると、行単位90/10分割やaugmentation等が入り得ます。今回のグループ分割と入力仕様を守る部分だけ再利用してください。引数はキーワード指定を優先し、位置引数による別パラメータへの誤代入を避けます。[R2]

---

## 3. データは900行程度まで。学習前に分割を固定する

件数はこの実験の予算案であり、性能に必要な最低量ではありません。グループを壊して件数を厳密に合わせる必要もありません。±10%程度の調整は理由を記録すれば構いません。

| 用途 | 行数の目安 | 使い道 |
|---|---:|---|
| train | 600 | 重みを更新する |
| dev | 100 | 終了時点・チェックポイントを選ぶ |
| holdout | 200 | モデル選択後、元モデルと学習後モデルを比較する |
| overfit用 | train内から最大64 | 学習経路の確認。追加の独立データではない |

今回は探索実験なので、calibrationセットをまだ作りません。holdoutはこの実験の未学習比較用であり、製品の最終性能保証に使うデータではありません。既存12問と英訳・反転版も引き続き**開発用診断**です。

### 3.1 対象タスクは二系統に限定する

**A. 判断基準切替**

同じcontextと候補を保ち、質問だけを変えると正解が変わる問題を作ります。例えば、費用最小と所要時間最小、容量最大と重量最小です。

- 値とプラン名の組合せを変える。正解がいつもA、いつも最後、いつも最短の候補文にならないようにする。
- 値を昇順に並べたcontextだけにせず、記載順も変える。
- 同率最小等は最初の実験では避け、正解を一意にする。
- 「属性を切り替える」問題と「同じ属性で最小／最大を切り替える」問題を含める。
- 初期の候補数は2～4程度でよい。既存のAPIの2～16候補対応は維持する。

**B. 明示ルール・境界・複合条件**

数値比較、境界値、AND / ORを含む短い問題を作ります。

- 閾値を20だけに固定しない。「未満」「以下」「超」「以上」を区別する。
- ANDの真偽の全組合せを含める。ORではANDと正解が異なる状態を含める。
- 偽の場合の動作も明示し、正解を一意にする。
- 回復アイテムの有無だけで答えが決まるような偏りを避ける。
- 同じ状態でルールだけを変えるペアを含める。「HPが低ければ常に回復」という常識ではなく、入力ルールを読まなければ解けない例にする。

説明用の対照例（新規holdoutには流用しない）：

```text
共通context：HPは20。回復薬を1個持っている。
共通候補：回復する / 待機する

質問1：HPが20未満、かつ回復薬があれば回復。それ以外は待機。
正解1：待機する

質問2：HPが20以下、かつ回復薬があれば回復。それ以外は待機。
正解2：回復する
```

この例に両方正解することと、別の閾値・別の表現でも正解することを区別します。

### 3.2 正解の決め方と品質確認

標準Pythonで構造化シナリオを生成し、通常の比較・論理演算で正解を決め、短い日本語テンプレートへ整形します。各系統に複数の言い回しを用意しますが、汎用自然言語生成システムは作りません。有料APIも不要です。

- 根拠となる構造化値、テンプレートID、生成seed、正解計算方法を保存する。
- 正解候補が存在し一意であること、単位、境界、否定、条件と結果の対応を検査する。
- 開発中はtrain/devから各系統・境界例を含めて合計20件程度を実際に読んで確認する。holdoutは学習前に機械的整合性を検査し、モデル出力を見て表現を調整しない。
- `program_verified` は実際にプログラム検証した項目だけに付ける。エージェントが作った文を人間が確認したことにしない。
- **正解計算コードはデータ作成・採点専用。推論側へif文で解答する仕組みを混ぜない。**

生成値から正しいラベルを計算できても、日本語への変換ミスまでは自動で保証されません。テンプレートの検査を省かないでください。

### 3.3 スキーマと分割

既存の学習データ形式を優先し、正解は `target.kind="hard"` と `target.choice_id` を使います。新たなスキーマ版や別のラベル体系は必要ありません。

同一シナリオの指示違い、候補順違い、言い換え、翻訳は同一 `group_id` にまとめます。必要な `pair_id` や `template_family` は既存のメタデータ領域、または小さな補助manifestへ保存すれば十分です。

- 先にgroupをtrain/dev/holdoutへ割り当て、その後に派生例を作る。
- groupの重複と、モデルに渡す入力内容の正規化hash重複を確認する。グループIDを変えただけの複製を通さない。
- ペアは片方を学習、もう片方をholdoutへ入れない。
- ラベル、根拠、source、task_family、正解入りのファイル名等はモデル入力へ混ぜない。
- 既存smokeの文章・英訳・数値だけを変えた近い派生を、新規holdoutとして数えない。

holdoutのうち約100行は、学習と同じ表現の系統だが異なる状態・数値を使います。残り約100行は、train/devで使わない表現テンプレート族へ割り当てます。**同型問題への転用**と**表現を変えた場合の転用**を別集計し、未知タスク一般への汎化とは呼びません。

二系統を概ね均等に含め、各系統・候補数に対して正解位置と正解ラベルの偏りを簡単に集計します。高度な分布最適化は不要です。

---

## 4. 学習実装はhard-labelのクロスエントロピーだけ

### 4.1 最小の損失

モデルの生logitsを `z`、正解候補の現在の位置を `y` とすると、使う損失は次です。

```text
loss = cross_entropy(masked_logits.float(), target_indices)
```

PyTorchのCrossEntropyLossは未正規化logitsを受け取り、単一正解では正解のクラス位置を指定できます。Softmaxを先にかけた確率をCrossEntropyLossへ渡さないでください。[R3]

GLiClassにも単一ラベル分類の分岐があります。ただし、読み込んだconfig、ライブラリ版、可変候補数の扱いを確認します。既存forwardのlogitsに対してERABI側で損失を計算する方法が最短なら、それで構いません。[R4]

今回は蒸留、温度学習、label smoothing、focal loss、contrastive loss、EWC等を追加しません。既存内部損失を利用する場合も、今回の損失へ想定外の項が加わっていないか確認してください。

### 4.2 候補順と候補マスク

学習時には候補をランダムに並べ替えてよいですが、候補ID・候補文・正解位置を連動させます。正解位置は並べ替え後にIDから求めます。元のデータを意図せず破壊的に書き換えないでください。

候補数が異なるバッチでは、存在しない候補を損失・予測対象から除外します。手元の実装で処理が複雑になるなら、最初は同一候補数をまとめるか小さなバッチにして構いません。候補数固定の新分類ヘッドへ作り替える必要はありません。

augmentationは、今回必要な候補順の入替え以外は無効にします。公開実装には候補削除や別文追加等もあるので、単一正解の意味を変える処理を既定で入れないでください。[R1]

### 4.3 推論コードをそのまま学習ループに呼ばない

入力整形は共用しますが、学習のforward/backwardを `no_grad` / `inference_mode` / `.detach()` 等で切断しないよう注意します。推論用パイプラインが `model.eval()` や勾配無効化を行う場合、その推論メソッド全体を学習用に流用しません。[R5]

学習時は `model.train()`、評価時は `model.eval()` とし、評価の後は学習モードへ戻します。最初の1更新で、損失が有限、必要なパラメータへ勾配が流れ、実際の重みが変わることを確認してください。

### 4.4 初期設定の案

| 項目 | 初期値・方針 |
|---|---|
| モデル | M1.5で取得したinstruct-base、同じrevision |
| 更新対象 | まず全パラメータ。採点部を再初期化しない |
| 入力長 | 整形後合計512トークン以内。暗黙の切り詰め禁止 |
| マイクロバッチ | 2を出発点に、空きVRAMに合わせる |
| 勾配蓄積 | 8を出発点（実効バッチ16） |
| 最適化 | AdamW、学習率2e-5、weight decay 0.01 |
| 学習率スケジュール | 初回は固定でよい。大きな探索をしない |
| 勾配クリップ | max norm 1.0 |
| 学習seed | 例42。値と適用箇所を記録する |
| DataLoader | Windowsではまずnum_workers=0 |
| 通常学習 | 最大5エポック。devでチェックポイント選択 |
| 過学習診断 | 最大300 optimizer steps。途中で目的達成なら終了 |

これらは推奨最適値ではなく、比較可能な最初の実験設定です。OOM時はまずマイクロバッチを下げます。入力を短くする場合は全文が上限内に収まる別データとして明示し、条件を黙って削らないでください。既存のGPUジョブを止めて空きを作りません。

学習用のパラメータはまずFP32で読み、必要ならautocastによる混合精度を使います。推論用BF16モデルをそのまま学習用の精度設計だとみなさないでください。FP16を使う場合のGradScalerと勾配蓄積・クリップの順序は、導入済みPyTorchのAPIで確認します。[R6]

LoRA、4bit optimizer、DeepSpeed等を最初から導入しません。低コストな調整でも学習できない場合は、制約を記録して止めます。推論時VRAMを学習時VRAMの保証に使わないでください。

勾配蓄積の最後に端数が出た場合の更新・損失の正規化も扱います。学習lossの表示値は蓄積用に割る前の尺度で記録し、見かけだけ低下させないようにします。

---

## 5. 段階A：64件以下への過学習

trainの各系統から、指示切替ペアを壊さず最大64件を選びます。dev/holdoutからは選びません。

最初は候補順を固定し、この小集合の学習前の正解数・lossを記録して学習します。50 optimizer stepsごと程度に、同じ集合を評価モードで測ります。

**確認したいこと：** 損失が下がり、同じ小集合の正答率が大きく上がり、保存後にもその状態を再現できること。

目安は95%以上（64件なら61件以上）ですが、これは学習配線を診断するための設計上の目安です。達しないだけでバグやモデルの限界が確定するわけではありません。300ステップ以内に十分な改善がなければ、損失・勾配・正解位置・入力・モード・学習率を点検します。修正または設定変更による診断の再実行は1回までを目安とし、無制限に続けません。

このcheckpointには `overfit_debug` 等の分かる名前を付け、**実用モデルと呼ばない**でください。保存後にモデルとトークナイザーを別プロセスで再読込し、同じ精度設定で代表例の全候補分布を比較します。許容誤差はdtypeに合わせます。

段階Bは、この過学習checkpointの続きからではなく、**元のinstruct-baseから改めて開始**します。

---

## 6. 段階B：600件程度で未学習問題への改善を測る

### 6.1 比較条件を固定する

比較するのは次の二つだけです。

- 元のinstruct-base（追加学習前）。
- 同じrevisionから600件程度を使って追加学習したモデル。

入力整形、候補セット、評価順、トークン上限、推論精度、採点方法を揃えます。学習後だけプロンプトや候補の文章を改良した結果を、「重みを学習した効果」として混ぜないでください。

段階Aで学習経路が成立していない場合は、段階Bへ進まず、その理由と実行済み結果を報告します。

### 6.2 学習とモデル選択

trainのみに候補順のランダム化を適用します。dev/holdoutは固定順です。まず1学習seedで実行し、総当たり探索や多数seedの実行は行いません。

各エポックでdevを測り、主に**指示切替ペアの両問正解数**、次に全体Accuracy、同点の場合NLLで保存対象を選びます。ルール系だけが落ちる等の偏りも記録し、総合値のみを見ないでください。

最大5エポック内の結果を残し、最良checkpointを確定してから、元モデルとそのcheckpointをholdoutで比較します。holdoutの成績を見て再学習・学習率変更・早期停止位置の選び直しをしません。

結果を見て設定やデータを変える次の実験では、そのholdoutは既に開発で見たデータと扱い、新しい評価を用意する必要があります。今回はそこで止め、次の案を一つ示せば十分です。

### 6.3 最低限の指標

既存evaluateを再利用し、必要なペア集計だけ小さく足します。

| 指標 | 記録する意味 |
|---|---|
| Accuracy | 正解数／問題数を併記する |
| ペア両問正解率 | 両問正解ペア数／対象ペア数。単に回答が変わった割合ではない |
| 系統別結果 | 基準切替、明示ルール、境界、複合条件等の内訳 |
| NLL / Brier | 正解率が上がっても悪化し得るので併記する |
| 順序感度 | 同じ問題の元順と別順で、IDに戻した予測が一致するか |
| 既存smoke | 学習前からの変化を開発用診断として表示する |

holdoutでは同じ表現族の新規問題と、未使用表現族を分けて集計します。ペア由来の行数と独立group数を両方残します。今回は有意差検定や高度な統計基盤は不要ですが、小差を確定的改善と表現しません。

順序感度は、モデル選択後のholdoutから事前ルールで選んだ20問程度で、元順＋固定seedの別1順を比較すれば十分です。全順列を列挙せず、予測一致率と両順での正答率を分けます。順序一致だけでは正しいとは限りません。

元の12問で失敗しなかった意図分類等が学習後に悪化していないかも確認します。ただし、この小集合だけで能力保持や忘却全体を断定しません。今回は忘却対策基盤を追加する段階ではありません。

### 6.4 確率の記録

NLLは正解候補の確率が極端に小さい例から強い影響を受けます。全体の値だけでなく、NLLの大きい例を数件確認してください。[R3]

NLL計算は可能なら生logitsのlog_softmaxから行い、JSON表示用に丸めた確率を使いません。既存実装が確率をclampしている場合はepsilon等を記録し、学習前後で方式を揃えます。変更が必要なら同じ方式で両方を再集計します。

今回も `calibration.status="none"`、自動採用未設定時の `review` を維持します。正答数の向上をもって確率校正済みにはしません。

---

## 7. テストを増やすのは、壊れやすい新規処理だけ

既存17件は維持します。件数・カバレッジの目標を設けず、重複テストは作りません。

新規のCPUテストは、未カバーのものがある場合に限り、次の3～4テーマ程度で十分です。

1. group分割の重複拒否と、同一シナリオの派生が同じsplitに残ること。
2. 候補順変更後も、IDから求める正解位置が正しいこと。
3. 学習用候補マスクとcross entropyが、小さな人工logitsで正しく動くこと。
4. データ生成の代表的な境界、AND/OR、一意正解の確認。

既存テストで対象を確認できている場合は再実装しません。大量のモック、全ての薄い関数の個別テスト、GPUモデルのダウンロードを伴う通常pytestは不要です。

実モデルの1更新・小集合への過学習・保存再読込・評価は、**手動実行の統合検証／実験**として扱います。確率・精度の揺れをCPUユニットテストの合否へ混ぜません。

---

## 8. 実装と保存は、既存構成への小さな追加にとどめる

基本は、データ生成・学習・比較の三処理で十分です。例えば `scripts/build_m2_data.py`、`src/erabi/train.py` と既存evaluateの小変更を使います。既に同等の機能があるならそれを再利用してください。

CLIサブコマンド追加が自然なら `train` を追加して構いません。CLIを大改造するより `python -m erabi.train` 等の単独入口が簡単なら、その方法でも構いません。実装していないコマンドを実行済みとして報告しません。

学習ループは短いPyTorch実装、または既存Trainerの最小利用のどちらか一つで十分です。両方を実装しないでください。新しいDB、実験管理サービス、設定フレームワーク、データ基盤は不要です。

保存先の案：

```text
data/m2_1/
  train.jsonl
  dev.jsonl
  holdout.jsonl
  manifest.json
runs/m2_1/<run_id>/
  config.json
  train_log.jsonl
  comparison.md
  metrics.json
  predictions/             # 既存形式に合わせてよい
  checkpoint/              # 再読込可能なモデル・tokenizer・config
```

manifestには生成seed、split/group数、テンプレート族、件数・hashを残します。configにはbase revision、ライブラリ版、精度、更新対象、optimizer、学習率、バッチ、蓄積数、seed、選択したepochを記録します。

checkpointは元キャッシュへ上書きせず、過学習診断版と通常学習版を区別します。保存数は最良と必要な最終版程度に抑え、エポックごとの大量コピーを残しません。使用したコマンド、実行終了コード、失敗理由も追えるようにします。

監査・比較スクリプトがエージェントの一時scratch領域だけにある場合、今回再実行に必要なものだけリポジトリ内へ移します。scratch全体をコピーする必要はありません。

速度計測は目的を広げず、学習前後で同じ短い入力・ウォームアップ・計時範囲を使った補助確認にとどめます。GPU処理の完了を含む計時にし、必要な同期処理を確認します。[R7] 過去の4問反転セットの速度だけを理由に、性能最適化の作業へ逸れません。

---

## 9. 結果の判断と停止条件

| 結果 | 今回の判断 |
|---|---|
| 小集合の学習も成立しない | 入力・損失・勾配・設定を点検。大規模学習へ進まない。能力限界とも断定しない |
| 小集合は覚えるが、holdoutで改善しない | 学習経路は成立。今回のデータ・設定では転用効果を確認できなかった |
| 同じ表現族では改善、未使用表現族では改善しない | 限定的な適応の兆候。汎用判断モデルの完成とは呼ばない |
| Accuracyは改善するが、ペア両問正解は改善しない | ERABIの中心課題である指示切替は未解決として残す |
| Accuracyは改善するが、NLL等が悪化する | 正答と確率品質のトレードオフを報告。自動採用や校正済み扱いに進まない |
| 両表現族・ペア・順序診断も改善する | この範囲で有望。より広い独立評価を次段階として提案する |

数件の差なら「小規模な傾向」とします。一般化を強く主張するための追加seedや広範な評価は、次の段階です。今回の予算を勝手に増やしません。

この実験の完了条件は「高い精度を出すこと」ではなく、**実際に更新・保存したモデルについて、未学習問題での学習前後比較と限界を示せること**です。改善しない結果も、そのまま有用な成果として残してください。

---

## 10. 完了時の報告

`STATUS.md` は過去の観測を消さず追記・更新し、ユーザーへの報告は次の内容を中心にしてください。

- 実行した範囲、未実行の範囲、ソースと設定の変更点。
- データの件数だけでなく、group数、表現族、分割・重複確認。
- 過学習診断の学習前後、更新step数、保存再読込結果。
- 通常学習の選択epochと、元モデル／学習後モデルの比較表。
- Accuracyに加え、ペア両問正解、同型／別表現の結果、NLL・Brier、順序感度。
- 改善した代表例と、残った失敗。観測と原因仮説を分ける。
- 再実行できるコマンド、リポジトリ内の成果物パス、次の一手を一つ。

外部から内容を確認できない `file:///...` リンクだけで終わらず、主要な比較値を報告本文へ含めてください。「実証」「必要」「十分」といった表現は、測定範囲に限定してください。

---

## 出所・技術参照

[F1] ユーザーがこの会話に貼り付けたM1.5実施報告。第1節の数値と観測はその転記であり、本書作成側による再実行結果ではない。

[F2] 既存 `ERABI_DESIGN.md`：学習データのhardラベル形式、group分割、段階的M2、既存構成の再利用。

[F3] 既存 `ERABI_M1_5_NEXT.md`：入力照合、JA/EN比較、診断用データの位置付け、テストを増やし過ぎない方針。

以下は2026-09-18に公開一次資料を確認したもの。mainブランチと公開ドキュメントは手元のインストール版と異なり得る。これらを理由に既存環境を更新しない。

[R1] GLiClass data_processing.py：prompt・候補順・augmentation・単一正解ラベル処理。
```text
https://raw.githubusercontent.com/Knowledgator/GLiClass/main/gliclass/data_processing.py
```

[R2] GLiClass train.py：公開学習経路、データ分割、設定、保存処理。今回の手順としてそのまま採用するものではない。
```text
https://raw.githubusercontent.com/Knowledgator/GLiClass/main/train.py
```

[R3] PyTorch CrossEntropyLoss：未正規化logits、正解クラス位置、負の対数尤度。
```text
https://docs.pytorch.org/docs/2.14/generated/torch.nn.CrossEntropyLoss.html
```

[R4] GLiClass model.py：problem_typeごとの損失とforwardのlogits。
```text
https://raw.githubusercontent.com/Knowledgator/GLiClass/main/gliclass/model.py
```

[R5] GLiClass pipeline.py：推論モード、入力整形、単一選択のSoftmax後処理。
```text
https://raw.githubusercontent.com/Knowledgator/GLiClass/main/gliclass/pipeline.py
```

[R6] PyTorch Automatic Mixed Precision examples：autocast、GradScaler、蓄積とクリップ。APIは導入済み版で確認する。
```text
https://docs.pytorch.org/docs/2.14/notes/amp_examples.html
```

[R7] PyTorch CUDA synchronize：GPU処理完了を待つ同期操作。
```text
https://docs.pytorch.org/docs/2.14/generated/torch.cuda.synchronize.html
```

本書の件数、学習率、step上限、採用目安、実験順はERABI向けの設計案であり、GLiClass開発元が保証する性能条件ではありません。本書は作業指示であり、学習済みモデルや実行結果を含みません。
