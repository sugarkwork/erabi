# ERABI — M3.2：正解データの是正と、修正効果を分けて測る再学習

作成日：2026-09-18  
対象：既存の erabi-local を実装する Codex / Antigravity  
位置付け：M3.1の実装報告を受けた追加指示。今回は「小規模マルチタスク化」ではなく、既知の正解ラベル誤りを切り分けて修正する。

## 0. 最初に読むこと・今回の範囲

AGENTS.md、STATUS.md、ERABI_M3_1_NEXT.md、ERABI_M2_1_NEXT.md、関連する実ファイルを読む。本書は、今回の作業範囲について優先する。将来フェーズをまとめて実装しない。

**今回の主目的は、生成器の正解ラベルを直し、同じ正しい評価問題で「元モデル・誤ラベル学習版・ラベル修正版」を比較すること。**

順番は次の通り。

1. 既存成果物を保全し、ラベル誤りの範囲を実ファイルで確認する。
2. 手で正解が確認できる少数例で生成器を直し、凍結済みデータの正解だけを是正した版を作る。
3. 旧モデルを新しい正解表で再採点し、以前の95%の意味を訂正する。
4. 学習用・開発用の修正が成立した場合、元のベースから通常学習を1回だけ実施する。
5. 同じ正しい問題集合で3モデルを比較し、小さな反実仮想診断と既知の退行を記録する。
6. 結果と小さなレビューバンドルを保存して終了する。

**今回は追加しない：** NLI・問い合わせ・偽指示防御等の学習データ混合、語彙の大規模拡張、新バックエンド、モデル交換、強化学習、蒸留、量子化、温度の再推定、API機能拡張、UI、DB、自動昇格、分散処理。

計画の提示だけで止めず、以下の範囲で確認・修正・実行・記録まで行う。ただし、ツールが要求する承認は迂回しない。既存GPUジョブを停止せず、課金API、外部公開、無断の依存更新、無関係なファイルの変更を行わない。

設計者が受領したのは報告Markdownだけであり、実装コード、実データ、重み、review_bundle.zip自体はこの文書作成時に未監査である。以下の報告値を実ファイルと照合し、相違があれば記録する。

## 1. 引き継ぐ観測と、訂正する解釈

### 1.1 報告されていること

| 項目 | M3.1報告の内容 |
|---|---|
| 対象の旧学習済み重み | runs/m2_1/trained_instruct_base/checkpoint |
| 誤り | build_m2_data.pyのサーバー問題で「大容量」に最小容量を正解として付与 |
| 具体例 | メモリ容量100/300/200GBに対し、大容量の正解が100GBになっていた |
| 旧評価 | M2.1 holdoutでも同じ生成器を使用。旧ラベルで190/200 |
| 新しい評価 | M3.1 final_testで96/200。校正前後で正答数は変わらない |
| 転用診断 | 元モデル18/32、旧学習済みモデル23/32 |
| 退行 | smoke-06、08、11が元モデル正解→旧学習済みモデル誤答と報告 |
| 校正 | Tが探索上限20へ到達、status=unstable、APIはT=1の未校正提供 |
| API | loopback限定、全件review、CLI一致、HTTP往復p50=24.45msと報告 |

以上は実装担当者の報告値。正解ラベルの誤り件数、他ドメインへの波及、実コードの動作はローカルで確認する。[F1]

### 1.2 報告の書き方

- 190/200は「旧ラベルとの一致」であり、自然文の意図に対する正答率の根拠にはそのまま使えない。一方、全問題・全学習効果が無効だったとも断定しない。修正後に再採点する。
- 95%と48%は同一データの前後比較ではない。語彙・テンプレート・正解基準等が違うため、差の全量を一つのバグの影響に帰属させない。
- 転用診断23/32という改善報告も残す。32問の小集合であり汎用性能の保証にはしない。
- 誤ラベルが存在することと、全退行・全高確率誤答・温度の境界到達の原因がすべて解明されたことは別。後者は未確定とする。
- 注意機構が特定語へ過剰に反応した、ルール学習と防御能力が必然的にトレードオフになった等は、出力だけから確定しない。
- T=20への到達は、その探索範囲の境界に達したという観測。温度が大きいこと自体を実装不良とはしない。Tが1～2になることを再学習の成功条件にしない。[R1]

前回指示書の第2.4節は、正解の系統的誤りを発見した場合に校正と「検証済みモデル」としての提供を止める条件を置いていた。今回の報告はその条件に該当する。既に得た結果は消さず、診断用成果物として保全する。以後は重大な不整合を発見した状態で「すべて正常」と完了判定しない。[F2]

## 2. 成果物を保全する

既存のdata/m2_1、data/m3_1、runs/m2_1、runs/m3_1の元データ・結果・重みを上書きしない。重みを削除したり、良い版だとして自動昇格したりしない。

既存方式で必要な入力・ラベル・設定・重みのhashと版を記録する。大きなモデルレジストリは不要。

修正版は例えば次へ保存する。既存レイアウトに合わせて変更してよい。

```text
data/m3_2_label_fix/
  train.jsonl
  dev.jsonl
  holdout_corrected.jsonl
  label_changes.jsonl
  audit_cases.jsonl
  manifest.json
runs/m3_2_label_fix/
  audit.md
  rescoring/
  trained/checkpoint/
  comparisons/
  notes.md
  review_bundle.zip
```

README / STATUSには旧学習版が「既知の誤ラベルを含む実験版」であることを追記する。既存APIは変更せず、全件review・自動実行なしを維持する。旧校正ファイルを新しい重みに付け替えない。今回は新しい校正を作らない。

## 3. 正解データの監査と最小修正

### 3.1 min/maxを、文章の意味と照合する

最初にbuild_m2_data.pyとbuild_m3_data.pyを読み、ドメイン、属性、判断基準、正解計算を対応表にする。新しいDSL、自然言語パーサー、汎用ルールエンジンは作らない。

例（実コードを確認して作る。下記は説明用）：

| 判断基準 | 選択方向 |
|---|---|
| 価格が安い | min |
| メモリ容量が大きい | max |
| 重量が軽い | min |
| メモリ容量が小さい | min |

必要なら既存の小さな設定に min/max を明示する。ただし「容量という属性なら常にmax」ではなく、質問の意味に対応させる。両方向を尋ねる設計なら質問ごとに方向を指定する。

正解を生成するメタデータや検証関数は、データ生成と採点専用にする。question/context/候補text以外の正解情報を推論入力へ入れない。学習したモデルの代わりにルール関数で予測を差し替えない。

### 3.2 共通バグを通さない、小さな独立正解表

今回必要なテストは、同じ生成関数の出力同士を比べるものではない。生成器を呼ばずに正解を決めた8～12ケース程度の固定表を用意し、当該関数と照合する。

少なくとも次を含める。

- 容量100/300/200で「大容量」なら300。
- 同じ値で「小容量」なら100。
- 値の割当を変え、最大・最小になる候補自体が変わる例。
- 他の既存ドメインの比較方向。
- 既存の境界条件・AND/ORで、分岐を取り違えると答えが変わる例。

期待値を本番の正解関数から算出してはいけない。自然文の要求と、数値・正解候補を短い根拠付きで点検する。生成器が返しただけのラベルをhuman_verifiedとしない。program_verifiedも、検証対象のバグと同じ関数による自己照合だけを根拠に付けない。

同率の扱いが曖昧なら、今回の対象データで同率が存在するか確認し、黙って先頭を正解にしない。意味から一意に正解を確定できない問題は、修正比較からの扱いを明記する。

### 3.3 学習済みデータは、できる限り正解だけを修正する

主実験では、元のtrain/dev/holdoutの問題文・question・context・候補text・候補順・ID・group・splitを保ち、誤っていたtargetだけを変える。来歴の追記はよい。

生成器を再実行して問題そのものまで変えない。原始レコードがあればそれと照合する。元の構造化データが残っていなければ、既知テンプレートの明示的な対応と値を確認して修正する。モデルの回答を正解決定に使わない。

label_changes.jsonlへ、問題ID、split、旧正解、新正解、理由、入力hashを記録する。全ドメインについて、変更件数と分母を集計する。誤り件数をドメイン比率から推測しない。

入力のhashが旧版と修正版で一致することを確認する。入力も修正しなければ意味が成立しない例があれば、ラベルのみ変更の例と別区分にして、単純な「ラベルのみ対照」を維持できるか判断する。

M3.1のcalibration/final_test/transfer_probeも、同じ基準で正解の妥当性を確認する。ただし、M3側にも同じバグがあるとは仮定しない。正しかった例を変更せず、修正が必要な場合は別版と差分を残す。

### 3.4 分割・固定パターンへの確認は一度だけ

group IDの分離だけでなく、同じ元シナリオ、入力hash、矛盾ラベルがsplitをまたいでいないか確認する。学習と評価が別の行であっても、共通の誤った正解関数を使えば今回の問題は起こり得る。

報告中の[100,300,200]が固定値なのか一例なのかは未確認。固定の候補名、数値の大小関係、属性の掲載順が、質問語から答えを決められる手掛かりになっていないか、コードと小さな分布集計で記録する。

主実験中にこの点まで大幅修正しない。ラベル修正効果と別の変更が混ざるためである。既存データの限定性として残し、第6節の小さな診断で確認する。

重大なsplit汚染、相反する正解、一意に修正できない問題などで対照実験が成立しなければ、再学習はせず、影響範囲と修正案を記録して終了する。無断で別の大規模データセットへ置き換えない。

## 4. 再学習の前に、旧結果を正しいラベルで再採点する

M2.1の旧holdoutについて、旧学習済みモデルの予測を修正後の正解表で再採点する。

入力hash、モデル識別子、候補ID対応が一致し、保存済みの生logitsまたは必要な精度の出力があれば再利用する。正答数は保存済みbestで判定できるが、NLLは丸めたJSON確率から再計算しない。不足分だけ同じモデルで再推論する。

少なくとも次を報告する。

| 集計 | 内容 |
|---|---|
| 旧ラベルでの成績 | 当時の190/200との照合。差があれば理由 |
| 修正ラベルでの成績 | 同一入力・同一モデルで正解表だけ変えた成績 |
| 正解変更対象 | 変更件数と、その部分集合での正答数 |
| 正解非変更対象 | 件数と正答数 |
| 変化 | 旧ラベルでは正解→修正後は誤答、その逆を別々に集計 |

ラベル修正後、ある指示切替ペアの正解が同じ候補になる場合がある。元の全ペアの両問正解率と、「正解が本当に異なるペア」に限った指示切替成績は分母を明記して区別する。比較のためだけに問題文や候補を変えてペアを作り直さない。

ここで新しい数値が出ても、「真の汎用精度」とは呼ばない。元の合成問題を是正した診断結果である。

## 5. 再学習は、元モデルから1回だけ

### 5.1 比較する三者

| 名称 | 内容 |
|---|---|
| B0 | 元の knowledgator/gliclass-instruct-base-v1.0。M2.1で使ったrevision |
| W_bad | runs/m2_1/trained_instruct_base/checkpoint。既知の誤ラベルで学習した旧版 |
| W_fix | 同じ元モデルから、正解だけ是正したtrainで新たに学習した版 |

**W_badの続きからW_fixを作らない。** 今回は誤ラベルの影響を分けたいので、元モデルから同じ方式でやり直す。

### 5.2 設定と予算

既存train.py、入力整形、collator、モデル構造、損失、評価処理を再利用する。M2.1の学習設定とseedを実記録から読み、原則同じ条件にする。

規模は元の600件・dev100件を維持し、通常学習1回、最大5 epochs、学習率2e-5を既存記録と照合する。optimizer、batch、勾配蓄積、precision、候補順シャッフル、checkpoint選択基準も記録して揃える。差が必要なら、ラベル以外の変更として明示する。

64件過学習診断を一から繰り返す必要はない。学習経路を変更していないなら、最初の更新と保存確認を既存方式で行うだけでよい。

修正devのみでcheckpointを選ぶ。既存holdoutやM3.1 final_testをepoch選択に使わない。比較の旧モデルも含め、T=1、同じ後処理・入力長・候補対応で評価する。

今回はcalibrationを実施せず、Tの値を学習目標にしない。学習が改善しないからといってLR探索や別seedの再学習を増やさない。環境不良等があれば原因と未実施を記録する。

## 6. 同じ正しい評価問題で比較する

### 6.1 評価集合

B0 / W_bad / W_fixを、同じラベル版・入力hash・評価コードで比較する。

1. M2.1 holdout_corrected：旧190/200の意味を修正し、ラベル是正による変化を見る。
2. M3.1 final_testの監査済み版：既に結果を見た回帰・転用診断として使う。新規の独立最終評価とは呼ばない。
3. 既存smoke12問とtransfer_probe32問の監査済み版：総合点だけでなく正解→誤答を記録する。
4. 次項の小さな反実仮想診断：最大24問。モデルの予測を見る前に入力・正解・採点方法を固定する。

既に見た評価集合を使ったモデル選択や修正判断は、開発上の比較として明示する。公開用の最終性能主張をする段階で、改めて独立評価を用意する。今回は新たな大規模final_testを増やさない。

### 6.2 候補順だけでなく、「事実が変われば答えも変わるか」を測る

以下は説明用の例。最大24問 / おおむね12組程度でよい。新しい汎用生成基盤を作らない。

```text
例1：同じ質問・候補名のまま数値を入れ替える
質問：最もメモリ容量が大きいサーバーを選ぶ。
条件A：A=100GB、B=300GB、C=200GB → 正解B
条件B：A=400GB、B=150GB、C=250GB → 正解A

例2：事実を固定し、判断基準を反転する
同じ容量100/300/200GB
大きい容量を選ぶ → B
小さい容量を選ぶ → A
```

候補名に正解が固定されていないか、質問だけに反応して本文を無視していないかを調べる。別の少数例では、本文に列挙した候補の順、他属性の値、無関係な条件を変えても、正しい根拠で選べるかを見る。

問題を作成する時点で正解の理由を明記する。入力に正解解説は含めない。今回のtrain/devには入れない。診断で失敗しても、その場で再学習しない。

これらは弱点を探す小さな診断であり、合格しても本文理解・推論能力を一般的に証明したとはしない。

### 6.3 記録する指標

既存のevaluateを使い、正解数/全件数、group数、ペアの分母、NLL、Brier、高確率誤答を出す。問題分布が違う集合を混ぜた一つの成績だけで判断しない。

ドメイン別、正解変更対象/非対象別、既存smokeの退行を併記する。古い丸め済みNLLと新しい生logits由来NLLを無注記で比較しない。モデル・入力・precision等が違うキャッシュを再利用しない。

簡単な目安として、候補数Kに応じた一様分布のNLL=log(K)、Brier=1-1/Kを各問題で計算して平均してよい。候補数を2個や3個と決めつけない。ランダム選択の期待正答率mean(1/K)と、決め打ちの先頭候補の実測正答率は区別する。

解釈例：

| 観測 | 読み方 |
|---|---|
| 修正対象が改善し、非対象も概ね維持 | 今回の対照条件で、ラベル是正の効果を確認 |
| 合成holdoutだけ改善し、反実仮想診断では失敗 | テンプレート等への依存が残る可能性。万能化成功とはしない |
| 偽指示や意図分類が退行したまま | ラベル修正だけでは直らなかった。次段階で保持用データ混合を検討 |
| 大容量以外にも誤りが残る | 一つのバグですべて説明できるとはしない |
| 改善しない | その結果を保存。自動で大規模学習や基盤変更へ進まない |

比較から学習メカニズムの全容が解明したとは述べない。単一seedの局所的な是正比較であり、モデル一般の性能評価ではない。

## 7. テストを増やし過ぎない

既存31件を件数目標として扱わず、意味があるものを維持する。追加は原則として次の2～3責務だけにする。

- 独立した固定期待値に対するmin/max・境界条件の正解計算。少数のパラメータケースにまとめる。
- ラベル修正版で入力・ID・group・splitを保持し、変更理由と旧新targetを記録すること。
- 既存splitテストに不足があれば、同一元問題・相反ラベルを小さなfixtureで検出すること。

同じ生成器を呼んで得た値を期待値にしない。網羅率、テスト件数、自然文問題の必須正答率をユニットテスト目標にしない。GPU学習とモデル精度測定は通常pytestから分離する。

API・校正・Web UIのテストをこの修正のために増築しない。既存APIの新たな致命的不具合が実ファイルで見つかれば、変更を必要最小限にし、ラベル修正と別に記録する。

## 8. APIと「完成」の表現

APIは現状の実験用機能を保ち、全件review、自動実行なし、外部公開なし。旧版/修正版の暗黙の切替を行わない。必要なら起動時の明示設定で選ぶだけとし、ルーターや動的ロードを追加しない。

HF_HUB_OFFLINE=1はhuggingface_hubのHub向け通信を止める設定であって、プロセス全体の通信遮断の証拠ではない。設定下でのキャッシュ起動と、実際に通信遮断して確認した起動を別に記録する。今回のためにPCのネットワーク設定を変更しない。[R2]

「asyncio.Lockがある」という説明だけで、競合時の即503まで独立確認済みとは書かない。実接続記録がなければ、その項目は未確認として残す。今回APIの大規模再監査はしない。

校正T=20は旧重み用の実験値であり、新しいモデルへ転用しない。Tを1～2へ収めることや、数値が大きいから上限を際限なく広げることを目標にしない。低いNLL、正答率、校正状態は別に評価すべきものである。[R1]

## 9. レビューバンドルと完了報告

次回は説明文だけでなく、根拠を見られる小さなZIPを用意する。既存のzip処理でよく、パッケージ配布基盤を作らない。

含めるもの：

- 関連する生成器・最小修正diff、学習・入力整形・評価コード、必要なテスト。
- 元の合成train/dev/holdoutとラベル修正版、M3.1の診断データ。秘密・個人情報を含まない合成データに限る。
- 旧新target差分、データmanifest、同じ問題での3モデル比較結果、反実仮想診断、誤答と退行表。
- モデルrevision/重みhash、依存版、学習設定、実行コマンド、STATUS/notes。

モデル重み、optimizer state、.venv、HFキャッシュ、秘密鍵、環境変数ダンプ、個人文書、無関係なフォルダは含めない。存在しないファイルを黙ってスキップするだけで完了とせず、含まれたファイルと不足項目を短く記録する。ZIPが実際に開け、内部ファイルがあることを確認する。アップロードしたとは書かず、保存したローカル相対パスを報告する。

### 完了時に報告すること

1. ラベル誤りの実際の件数・ドメイン・split別の範囲と修正内容。
2. 旧190/200を正しいラベルで採点し直した結果。入力不変か、評価定義を変えたかも明記。
3. B0 / W_bad / W_fixを同じ正しい評価セットで比較した結果。
4. 改善した例、残った誤答、正解→誤答の退行、反実仮想診断。
5. 実施した学習回数・データ量・設定、追加/変更ファイル、テスト結果。
6. まだ確認できていない事項と、review_bundle.zipの保存先。

ラベルの是正だけで解決しない場合も、範囲を分けて記録できれば今回の成果になる。次の段階の候補は結果から一つだけ提案し、今回は実行しない。

## 10. 出所と設計判断

[F1] ユーザー添付「貼り付けたマークダウン（1）(2).md」のM3.1報告。特に「生成器の正解条件と自然文の不整合」「独立最終評価」「転用診断」「未確認事項」。報告内容と、この指示書の提案・推論を区別する。

[F2] 既存ERABI_M3_1_NEXT.md：第2.4節（重大な不整合時の停止）、第4節（校正）、第5節（実験用API）。

[R1] Guo et al., On Calibration of Modern Neural Networks, ICML 2017。校正の背景。特定のTの値やERABIでの成功を保証する資料ではない。
```text
https://proceedings.mlr.press/v70/guo17a.html
```

[R2] Hugging Face公式 huggingface_hub Environment variables、HF_HUB_OFFLINE。
```text
https://huggingface.co/docs/huggingface_hub/en/package_reference/environment_variables
```

外部資料確認日：2026-09-18。本書の件数・実験順・停止条件はERABI向けの設計判断。今回の文書作成時にモデル訓練やソースコード監査を実行したという意味ではない。
