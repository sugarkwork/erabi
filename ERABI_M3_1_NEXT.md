# ERABI — M3.1：限定評価・確率校正・review固定のローカルAPI

作成日：2026-09-18  
対象：既存の erabi-local を実装する Codex / Antigravity  
前提：M2.1の実装担当者による完了報告を受領。設計者はローカルのコード・データ・重み・生の評価結果を未監査。以下の数値は報告値であり、再現確認済みの数値ではない。

## 0. 今回の判断と作業範囲

**M3へ進む。ただし「汎用判断モデルの実用化完了」ではなく、「既存の学習済みモデルを、限界が分かる実験用ローカルAPIとして使える状態にする」段階とする。**

既存の `AGENTS.md`、`STATUS.md`、`docs/ERABI_DESIGN.md`、`ERABI_M2_1_NEXT.md` と該当コードを読む。本書はそれらの技術契約を置き換えず、今回の範囲を具体化する。過去文書の全将来機能を一括実装しない。

今回の順番：

1. 既存成果物を使う短い整合確認と、退行・誤答の記録。
2. モデル重みを固定したまま、校正用・最終評価用の新規データと小さな転用診断を用意。
3. 単一温度の校正を行い、校正前後を分離したデータで評価。
4. 校正の成否にかかわらず、問題のない推論経路を薄いローカルHTTP APIとして提供。全件reviewのままにする。
5. 実行結果・限界・再現に必要な小さな成果物を保存して終了。

**今回はモデルの再学習をしない。** 大量データ、マルチタスク学習、ModernBERT、蒸留、強化学習、量子化、LoRA、別モデル比較、モデルルーター、UI、DB、分散処理、実験管理サービスは追加しない。

計画の提示だけで止めず、この範囲の実装・実行・記録まで進める。ただし、ツール側で要求される承認は迂回しない。有料サービス、外部公開、既存環境の破壊的変更等が必要なら、その操作は行わず制約を記録する。GPUを使う既存ジョブを停止しない。新規依存は必要なものだけ `.venv` に入れ、既存PyTorch等を無断更新しない。

## 1. 引き継ぐ報告と、その適用範囲

| 項目 | M2.1の報告値・内容 |
|---|---|
| ベース | knowledgator/gliclass-instruct-base-v1.0 |
| 今回の対象重み | runs/m2_1/trained_instruct_base/checkpoint |
| 学習 | 600 cases / 300 groups、最大5 epochs、lr=2e-5 |
| 開発用 | 100 cases / 50 groups |
| holdout | 200 cases / 100 groups |
| holdout Accuracy | 96/200 → 190/200 |
| 指示切替ペア両問正解 | 16/100 pairs → 90/100 pairs |
| holdout NLL / Brier | 1.8035 / 0.8308 → 0.2937 / 0.0886 |
| 同型 / 未使用表現族 | 学習後100/100 / 90/100 |
| 既存smoke | 7/12 → 8/12 |
| 順序変更診断 | holdout先頭20件、元順・反転とも20/20、best一致20/20 |
| 推論p50 | 22.44ms → 21.97ms。測定範囲はローカル実装で確認 |
| 過学習診断 | 64/64、75 optimizer steps、NLL表示0.0000 |

解釈を以下に限定する。

- 今回の合成データ生成・分割・採点が妥当なら、二系統の問題分布内で明確な改善があったという、有望な結果である。
- 未使用表現族90%は、当該生成方法で用意された未使用表現族での成績。未知の仕事・自由な日本語・任意の条件文で90%を保証しない。
- 20件で順序変更に一致した事実を、全入力での位置不変性の証明と呼ばない。候補順シャッフルが改善の原因と断定するには別の対照実験が必要だが、今回はそのための再学習をしない。
- M1.5報告では `smoke-11` はinstructの日本語で正解、M2.1報告では誤答。まず同じ入力・正解であることを確認し、一致していれば実際の退行として記録する。総合7/12→8/12を「忘却なし」に読み替えない。
- `smoke-05` の失敗は、問題内にHP等のルールと必要情報が全部あれば、外部ドメイン知識不足と確定できない。条件の組合せ、語彙、入力形式などの転用の不足という未確定の仮説に留める。
- DeBERTaやattentionが具体的に何を獲得したかは、今回の成績だけから解明したとは書かない。

`STATUS.md`の過去の成績を消さず、断定の訂正・観測の範囲を追記する。

## 2. 最初の整合確認：既存成果物を使い、実験を広げない

### 2.1 結果とデータの小さな監査

`build_m2_data.py`、学習時のデータ読み込み、入力整形、評価コード、predictions/metricsを確認する。監査のために新しいフレームワークを作らない。

確認事項：

- 推論に渡るのはquestion/context/候補textであり、target、正解解説、group_id、task_family、品質ラベル、外部ID等がモデルの入力へ漏れていない。
- group_idが違うだけで安心せず、元シナリオの対応と整形前入力の正規化hashの重複も確認する。同じ問題の別IDによる重複や、同じ入力なのに異なる正解があれば記録する。
- 「未使用表現族」は実際にどのテンプレートID・文章表現かを数例示す。数値を変えただけの新問と、言い回しを外した新問を区別する。
- 生成器の正解条件と自然文が一致するか、各系統から代表2～3 groupだけ目視確認する。特に `<`/`<=`、AND/OR、例外条件、同率の最小・最大に注意する。
- 候補textや配置が正解に固定対応する生成になっていないか、簡単な集計とコード確認で見る。候補IDの無意味な変更を候補文の変更と混同しない。
- holdoutで再学習・epoch選択をしていないこと、段階Bが元のベースから開始されたことを記録で確認する。

group分離のテスト通過は大切だが、それだけで全種類の漏洩がないとは証明しない。以上は異常があるという指摘ではなく、報告値の解釈を支える最小限の確認である。

### 2.2 誤答10件とsmokeの変化を保存する

既存のholdout学習後誤答10件を、問題ID・group・系統・表現族・正解・予測・全候補確率・NLLとともに一覧化する。すべて長文引用する必要はなく、入力を追える相対パスでよい。

既存smoke12件は、M1.5のベースと学習後の予測をIDで突き合わせ、次を表にする。

- 正解→正解
- 誤答→正解
- 正解→誤答
- 誤答→誤答

`smoke-11`が同じ問題で本当に正解→誤答なら、退行として明記する。総合得点や特定タスク数例だけで退行を打ち消さない。過去予測ファイルが足りない場合に限り、同一条件で不足分を一度推論する。

この時点で誤答を学習データに足さない。現在のholdoutは以降「結果を確認済みの診断集合」であり、今回の温度推定にも新規最終評価にも流用しない。

### 2.3 NLL 0.0000の意味を確認する

0.0000は表示丸め、有限精度での飽和、あるいは極めて小さい値の可能性がある。数字だけでバグや漏洩とは断定しない。一方で「不確実性が消えた」とも言わない。

- 表示前のNLLを科学表記でも保存する。
- NLLは、表示用に丸めた確率を再読込して計算しない。生logitsから `log_softmax` を使って求める。[R1]
- BF16/FP16でforwardする場合も、後処理は最低FP32に変換してから実行する。校正の小さなCPU計算はFP64でもよい。
- 実候補のlogitsにNaN/Infがないか確認する。paddingに意図的に入れた `-inf` と実候補の異常を区別する。
- clampでNLLの上限が変わっていた場合は方式を明記し、比較対象を同じ定義で再集計する。以前の数値を黙って置き換えない。

段階A・Bの再学習は不要。既存モデルから必要なlogitsを取得して確認すればよい。

### 2.4 ここで止める条件

正解情報の入力混入、重大な重複、正解の系統的誤り、評価の別モデル参照等が見つかった場合は、確率校正と「検証済みモデル」としての提供を止める。原因・影響範囲・最小修正まで記録し、自動で再学習や大量の実験へ進まない。

単なる不得意問題や少数の退行は、記録して先へ進める。モデルの万能性は今回のAPI化の必要条件ではない。

## 3. データを用意する：校正と検証を分離する

以下は今回の実験予算の目安であり、実用精度を保証する件数ではない。

| 新規集合 | 目安 | 役割 |
|---|---:|---|
| calibration | 200 cases / 100 groups | 温度Tだけを決める |
| final_test | 200 cases / 100 groups | 校正前後を最後に一度比較する |
| transfer_probe | 最大32 cases | 小さな言い換え・文脈変更・退行の診断。校正には使わない |

既存の生成器を小さく拡張して使う。汎用的なデータ生成エンジンへ作り直さない。全問にペアを作るため意味の不自然な問題を作らず、件数より正解の明確さを優先する。

### 3.1 calibrationとfinal_test

- M2.1のtrain/dev/holdoutと、新規2集合の間で元シナリオ・正規化入力hashを分離する。新しいseed・group_idだけを付けて同一問題を再利用しない。
- 系統A/B、表現族、候補数の分布を推論前に固定する。calibrationとfinal_testの構成は揃える。同じ問題・その翻訳・並べ替え版は同一groupとして扱う。
- M2.1で確認した表現の一部を今回の校正分布へ含める場合、その事実を記録する。新規シナリオであっても、完全に初めての表現とは呼ばない。
- 校正に使う分布は合成ルール判断の範囲である。自由な日本語全般や、問い合わせ分類/NLI/プロンプト攻撃への校正を保証するものではない。
- モデルを見て正解しやすい問題だけ残さない。不正問題の修正は理由と版を残す。
- 新規最終評価を読んだ後に温度、データ配分、入力整形、モデルを再調整し、そのまま同じ集合を「未使用」として評価し直さない。

現モデルと校正は実験対象として固定する。最終評価が悪い場合は結果を報告し、採用を保留する。最終評価を使う合否判断と、モデル・温度の再調整を区別する。

### 3.2 小さなtransfer_probe

同じ生成テンプレートの数値差分だけでは測れない転用を、最大32問で見る。次のような観点を少数ずつ含めればよい。

- 同じルール構造を、別の語彙・場面で記述する。HPから在庫等へ変える場合も、判断に必要なルールは問題内にすべて与える。
- 「小さい側を選ぶ」だけでなく、問題で示した対応を反転し、同じ質問語でも別候補が正解になる組を作る。
- 条件の順序、否定、境界、AND/ORや例外の表現を変える。単なる表記の違いか、正解を変える操作かを区別する。
- 本文内の引用・偽指示と外側の判断指示を区別する問題、および意図分類等の小さな保持診断。

作成後に根拠と正解を点検してから、ベース・学習後を各1回評価する。モデルの出力を見て問題を作り直さない。LLMが作成・自己点検しただけの問題を `human_verified` としない。コードで検証できる範囲のみ `program_verified` とし、残りはレビュー方法を記録する。

この小集合の成績は「汎用ベンチマーク」ではなく、転用・退行を確認する診断結果とする。悪くても、今回は修復のための再学習を始めない。

### 3.3 順序感度の追加確認は小さく

既存の先頭20件の結果はそのまま残す。新規final_testから、結果を見ない固定ルールで系統・表現族に偏らない最大20問を選び、元順と固定seedの別1順を比べる。既存の診断コードを再利用する。

best一致率、両順での正解数、対象group数を記録する。全順列、学習seed比較、シャッフル無効での再学習は行わない。ここでも「位置不変性が確立」とは書かない。

## 4. 単一温度の校正

重みを更新せず、校正用データの生logitsと正解だけを保存し、CPUで温度を調整する。正解から推論結果を差し替えるコードを推論経路へ入れない。

```text
p_i = softmax(z_i / T), T > 0
NLL = mean(-log_softmax(z / T)[正解位置])
```

温度スケーリングは既存の後処理手法であり、確率の調整を狙うもの。[R2] 正の単一Tは数学的に各問題の候補順位を変えない。したがって、同じlogitsから計算したAccuracyとペア正答率が変わるなら、丸めによる同率判定等も含め実装を確認する。

### 4.1 実装を小さくする

- 初期T=1。最適化する変数はlog(T)等の1個だけ。
- 既存PyTorch等で十分。校正専用の大型ライブラリを追加しない。
- 数値保護のための探索範囲をあらかじめ決める。例として0.05～20。これは既知の最適範囲ではなく今回の設計上の制約。
- 最大100回程度の目的関数評価を目安にし、無制限の探索をしない。T=1の目的値を必ず比較候補に含める。
- 非有限値、境界への張り付き、全問正解で温度を小さくするほど良くなる等の場合、校正が安定したとは言わない。状態を記録し、未校正T=1運用を選べるようにする。
- 候補数が違う場合、実候補だけを対象にNLLを計算する。paddingを学習対象や正解候補にしない。
- 校正用のNLLが改善しただけで成功確定とせず、固定したTをfinal_testで評価する。

### 4.2 保存・適用範囲

既存設計の `calibration.json` を使い、少なくとも次を含める。[F1]

- 正の有限temperature、artifact_id、作成日時。
- モデル重み・configを識別するhash。複数重みファイルなら対象を明示した小さなmanifestでよい。
- tokenizerと入力整形の版またはhash、forwardと後処理のprecision、入力上限。
- calibrationデータのhash、生成方法・版・対象タスクと候補数の説明。
- 計算方法、最適化の終了状態、T=1との比較。

hashは実ファイルから計算する。親モデルの公開IDだけで学習後重みを同一視しない。DBやモデルレジストリは不要。起動時に1回確認し、毎リクエストで巨大ファイルをhashしない。

モデル・入力整形・precision等が不一致なら校正付き起動を失敗させる。校正を使わない起動は明示設定で選び、`calibration.status=none`を返す。黙って別モデル用の校正を付けない。

`applied`は「温度を適用した」という事実であり、「この入力の確率が正しいと保証した」という意味ではない。今回のモデルは未知の用途を確実に検出できない。分布が変わると事後校正の有効性も損なわれ得る。[R3]

### 4.3 最終評価と採否

学習後モデルのT=1と校正後Tを、同じfinal_testで一度比較する。少なくとも以下を出す。

- 正解数/問題数、独立group数、指示切替ペア両問正解。
- NLL、`mean(sum_i((p_i-y_i)^2))`としてのBrier。
- 確率帯ごとの件数・平均pmax・実際の正解率。既存のbin分割を再利用する。
- 誤答例のpmaxと正解確率。校正後も高確率で間違える例を隠さない。
- 事前に固定した閾値（例0.8/0.9/0.95）での仮の採用件数、coverage、誤答件数、selective risk。

最後の閾値比較は診断専用で、自動採用設定を有効にしない。採用0件のriskは0%ではなくnull。派生ペアを独立な試行として誤り率を保証しない。複雑な区間推定基盤は今回不要。

校正後の成績が悪い、数値的に不安定、または適用範囲が十分に説明できないなら、校正ファイルは実験成果物として残し、APIは明示的な未校正モードで提供してよい。最終評価を見ながらTを再探索しない。

## 5. 薄いローカルHTTP API

### 5.1 境界

- バインド先は `127.0.0.1:8765`。LAN公開、Tailscale公開、トンネル、ポート開放はしない。
- 1 worker、reloadなし、学習済みモデル1個だけ。起動時に1回ロードし、各リクエストでロードし直さない。
- FastAPIを使う場合、新規実装はlifespanでモデルの初期化・終了処理を行う。[R4] モジュールimportだけで実モデルをロードしない。
- 同時GPU推論は1件。既存方針に合わせ、小さな排他制御で処理中は503 busy。ジョブキューや分散ロックを作らない。
- 既存のCLI推論・入力検証・全候補後処理を共用する。API用の別推論ロジックを書かない。
- モデル・tokenizerはローカルの固定ファイルから読み、必要に応じてHFのオフライン設定を使う。起動中の自動ダウンロードを前提にしない。
- ブラウザUIは作らない。Swagger/ReDocは今回無効でよい。CORS全許可を追加しない。

loopbackへの限定は利用範囲の制限であり、同一PC内の全プロセスに対する強い認証の代わりではない。完全なセキュリティを保証したとは書かない。リクエストから任意モデル・URL・ローカルファイルをロードする機能を設けない。

### 5.2 エンドポイントと応答

既存実装に同等のAPIがなければ、`GET /health` と `POST /predict` の二つだけでよい。モデルロード失敗時は起動失敗とし、正常なhealth応答を返すよう偽装しない。

リクエスト・レスポンスは `schema_version=1` の既存契約を維持する。新しく別のJSON形式を作らない。

レスポンスでは全候補と確率を入力順で返し、best_candidate_idを付ける。ただし **全件、以下のdecisionを維持する**。これはレスポンス全体ではなく該当部分の例である。

```json
{
  "decision": {
    "status": "review",
    "reason": "policy_not_configured"
  }
}
```

校正付きなら `calibration.status=applied` と実artifact_id、明示的な未校正モードなら `none` とnullを返す。候補名が「実行」「承認」等でも、それ自体を行動の許可と扱わない。

今回accept切替を実装・有効化しない。best_candidate_idはモデルの予測であり、APIが要求の実行を承認したという意味ではない。呼び出し元にもreviewを無視して副作用を実行させない。

### 5.3 入力と失敗

既存の2～16候補、候補ID対応、NFC重複検査、UTF-8リクエストサイズ上限65,536 bytes、整形後512 tokens、特殊トークン拒否、無断切り詰め禁止を引き継ぐ。[F1]

不正入力422、リクエストサイズ超過413、busy/OOM等503、非有限モデル出力500など、既存のエラー契約を再利用する。生本文・秘密情報・スタックトレースを外部応答に含めない。入力の常時保存はしない。

失敗時に一様分布・固定の答えを成功として返さない。GPU失敗時の無断CPUフォールバックや外部API呼び出しも行わない。新しい防御ミドルウェア一式を作るのではなく、このAPIに必要な最小限の検証を実装する。

### 5.4 実モデルでの接続確認

別プロセスでサーバーを起動し、標準のローカルHTTPクライアントから正常2～3件と不正1件程度を送る。CLIと同じ入力・precision・temperatureで全候補分布が対応することを見る。許容差はdtypeに合わせ、不要なbit完全一致を要求しない。

HFオフライン設定とローカルファイルで動いたこと、実際の通信を遮断して確認したことは別に記録する。既存ジョブを壊すLAN切断やOSのネットワーク設定変更はしない。起動したテストサーバーだけを終了し、他のPythonプロセスをまとめてkillしない。

必要なら代表1問のwarmup後に20回程度のHTTP往復を測定し、中央値と範囲を残す。報告されている約22msはそのままAPI往復時間だと扱わない。サーバー起動時間と通常応答時間を区別する。網羅的ベンチマーク基盤は作らない。

## 6. テスト：追加する責務を限定する

既存21件は必要性に応じて維持する。件数、網羅率、カバレッジ100%を目標にしない。今回は不足する次の責務だけを、小さなtensorやfake scorerで補う。

| 責務 | 確認内容 |
|---|---|
| 温度と数値処理 | T=1との整合、T<=0/NaN/Inf拒否、実候補へのマスク、正の単一Tで非同率のbestが変わらない |
| 校正とモデルの結合 | 小さなダミーファイルでhash一致/不一致、明示的な未校正モード |
| HTTP契約 | 全候補・ID・順序、review固定、異常を成功扱いしない |
| 起動と終了 | fake scorerを1回だけロード。校正不一致を起動時に拒否 |

既存テストと重複するケースを大量追加しない。lifespanを含むAPIテストには必要なら `with TestClient(app)` を使う。[R5] 通常のpytestでモデルダウンロード、GPU確保、学習をしない。

既知の日本語問題に高確率で答えることをCPUユニットテストの条件にしない。意味の正しさ・転用・順序感度は実験として測定する。

## 7. 最小成果物と終了条件

新規コードは既存構成を優先し、例えば `calibrate.py`、`serve.py`、既存生成スクリプトの小拡張で十分。DB・registry・抽象backend・自動昇格機構を作らない。

成果物の案：

```text
runs/m3_1/
  audit.md                    # 整合確認、既知の退行、NLL表示の確認
  data_manifest.json          # split/group/生成版/hash/適用範囲
  calibration.json            # Tの推定が成立した場合。候補ファイルと採用を区別
  eval_final_before/           # 既存evaluate形式を再利用
  eval_final_after/
  transfer_probe_results/     # ベースと学習後の比較
  notes.md                    # 実行したコマンド・成否・未実行
```

データは既存のdata配下、logitsはruns配下等、既存配置に合わせる。必要な診断スクリプトをAntigravityの一時scratch内だけに残さず、再実行が必要なものだけリポジトリ内へ置く。READMEには実際に動いた起動コマンドとPowerShellでの呼び出し例を残す。

完了報告には次を含める。

1. 監査範囲と見つかった問題、修正内容、未確認事項。
2. smokeの正解→誤答を含む差分、holdout誤答の傾向。
3. 新規calibration/final_testのcase数とgroup数、分割・正解確認方法。
4. T、推定の状態、独立final_testでの校正前後のNLL/Brier等。良くない指標も省かない。
5. 小さな転用・順序診断と、説明できる適用範囲。
6. ローカルAPIの起動・呼び出し・review固定・CLI一致・オフライン確認の実績。
7. テスト結果、成果物の相対パス、未実行項目、次の課題を一つ。

**温度を採用できなかった場合も、理由を示し未校正・review固定のAPIが動けば、今回の工学的な到達点として成立する。** 逆に、APIが動いただけで汎用判断能力や誤り率の保証が得られたとは扱わない。

### 次回レビュー用の小さなZIP

可能なら既存zip機能で `runs/m3_1/review_bundle.zip` を作る。新たなパッケージング基盤は不要。

含めるのは、STATUS.md、本段階のnotes/audit、データ生成・推論・評価・校正・APIの関連コード、必要な設定、合成評価データと予測・集計などの小さなファイルだけ。M1.5/M2.1のsmoke予測もあれば含める。使用したモデルrevision・hashと依存版を記録する。

`.venv`、モデル重み、HFキャッシュ、学習optimizer state、APIキー、環境変数ダンプ、個人の文章、無関係な作業フォルダは入れない。ZIPを作ったことと、外部へアップロードしたことは区別する。完了報告ではローカルの保存先を示せばよい。

## 8. 参照資料

[F1] 既存 `docs/ERABI_DESIGN.md` 第6節、第12～14節、および `ERABI_M2_1_NEXT.md`。プロジェクトの入力・出力・校正・保留・テスト契約。M2.1の成績は今回ユーザーが貼り付けた実装担当者の報告が出所であり、これら設計資料で実測されたものではない。

[R1] PyTorch公式 `torch.nn.functional.log_softmax`。logとsoftmaxの別計算より数値的に安定した計算、dtype指定。参照ページは2.9だが、手元の導入版を無断更新せず実装を照合する。
```text
https://docs.pytorch.org/docs/2.9/generated/torch.nn.functional.log_softmax.html
```

[R2] Guo et al., On Calibration of Modern Neural Networks, ICML 2017。温度スケーリングの背景。ERABIでの有効性を保証する資料ではない。
```text
https://proceedings.mlr.press/v70/guo17a.html
```

[R3] Ovadia et al., Can You Trust Your Model's Uncertainty? Evaluating Predictive Uncertainty Under Dataset Shift, NeurIPS 2019。分布変化と確率評価の背景。今回のモデルや合成データを直接評価した研究ではない。
```text
https://arxiv.org/abs/1906.02530
```

[R4] FastAPI公式 Lifespan Events。共有モデルの起動時ロード・終了時処理。
```text
https://fastapi.tiangolo.com/advanced/events/
```

[R5] FastAPI公式 Testing Events。TestClientのコンテキスト内でlifespanを実行する方法。
```text
https://fastapi.tiangolo.com/advanced/testing-events/
```

外部資料確認日：2026-09-18。データ件数・チェック範囲・実装境界はERABI向けの設計判断であり、各論文やライブラリが規定したものではない。
