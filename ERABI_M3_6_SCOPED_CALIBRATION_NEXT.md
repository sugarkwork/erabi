# ERABI — M3.6：重みを固定した限定用途の確率校正と新規評価

作成日：2026-09-18  
対象：既存の `erabi-local` を実装する Codex / Antigravity  
位置付け：M3.5の学習予算比較を受けた次の作業指示。設計書を全面的に置き換えない。

## 0. 今回の結論・作業範囲

**W_v2_ce10を今回の実験対象として固定し、合成ルール判断という限定用途に対して、独立した校正用データと新規評価用データで単一温度を検証する。再学習は行わない。**

元の `AGENTS.md`、`STATUS.md`、`docs/ERABI_DESIGN.md`、M3.5の実行結果を読む。過去の将来機能を一括実装しない。既存の推論・評価・校正処理を再利用する。

この指示書の作成者が今回受領したのはM3.5の報告Markdownであり、M3.5版ZIP・重み・個票は未受領。以下の成績とハッシュは**実装担当者の報告値**である。ローカルの実ファイルで照合するが、新しい大規模監査を始める必要はない。

今回行うのは次の範囲だけ。

1. 既存成果物の小さな照合・報告上の訂正。
2. 既存の二系統を対象とする、新規calibrationとfresh_evalの作成・正解確認・分離確認。
3. 固定重みのlogitsから、正の単一温度Tを一度推定。
4. 新規評価と既存の転用診断を**別集計**し、校正前後・限界を記録。
5. 採用条件を満たせば校正ファイルを実験用候補として保存し、既存APIとの小さな接続確認。全件reviewを維持。

今回は、追加エポック、別seedでの学習、ペア損失、マルチタスク学習、モデル交換、蒸留、量子化、未知入力検出器、モデルルーター、UI、DB、分散処理、APIの新機能を追加しない。有料API、サービスの外部公開、既存GPUジョブの停止、依存関係の一括更新も行わない。

計画の提示だけで停止せず、この範囲の実行・結果保存まで進める。ただしツールや環境が要求する承認を迂回しない。重大な不整合や必要ファイル不足が残れば、その部分を止め、根拠と完了できた範囲を報告する。

## 1. 引き継ぐ観測と、解釈の範囲

| 項目 | 5ep / W_v2_gradfix | 10ep / W_v2_ce10 |
|---|---:|---:|
| eval_v2 正解数 | 162/200 | 195/200 |
| eval_v2 NLL / Brier | 0.4761 / 0.2755 | 0.1538 / 0.0484 |
| 正解が異なるペアの両問正解 | 12/47 | 43/47 |
| うちgoal_followingの切替ペア | 3/31 | 31/31 |
| trainのgoal_following切替ペア | 10/97 | 97/97 |
| devのgoal_following切替ペア | 0/19 | 19/19 |
| transfer_probe 正解数 | 25/32 | 25/32 |
| transfer_probe NLL / Brier | 0.6738 / 0.3875 | 1.6239 / 0.4754 |
| smoke 正解数 / NLL | 8/12 / 1.5148 | 9/12 / 2.1239 |

今回の対象重み：`runs/m3_5_ce10/trained/checkpoint`。

報告された `model.safetensors` のSHA256：

```text
10ep: 6c9cac0610798cb031cbc6513b3e7483d4e130ade7bf9da07818e23c96d39b51
first5: 631a6d8a60844c002227b31eb8bc36f24c7ae2d2842554290c0b725feb2e04c7
```

解釈を次に限定する。

- このデータ・モデル・seed・評価では、通常CEの学習回数を延ばして、指示切替の成績が改善した。独自の対照損失がこの改善に不可欠だったとは言えない。
- 6～7epでの正答数の急増は観測。attentionがその時点で質問へ移った、二段階の内部機構が実証された、最初の5epで一切学習していなかった、という結論には内部分析がない。追加の内部分析は今回は不要。
- `eval_v2`、smoke、transferは既に反復的な開発判断に使った診断集合。重み更新に直接使っていなくても、今後「手つかずの最終試験」とは呼ばない。
- 31/31は特定の切替ペア部分集合の成績。任意の日本語・任意の選択肢・未知タスク全体が100%という意味ではない。
- 同一ファイルハッシュの報告は当該2ファイルの一致の根拠。すべての環境・GPU・実行で再現する保証ではない。
- transferのNLL/Brier悪化を「確率品質も維持」と書かない。過信の説明は個票の確認と区別する。

## 2. 最初の照合は保存済みファイルで済ませる

### 2.1 モデルと評価結果

M3.5の `train_summary.json`、比較集計、個票、ローカル重みを照合する。実際の採用epochと重みファイルhash、tokenizer/config、入力整形版、推論precisionを記録する。ハッシュだけ一致していても入力整形まで同一とは仮定しない。

保存予測があれば、eval_v2の正解195件・誤答5件、切替43/47、transfer25/32、smoke9/12を再集計する。正解の根拠は正解データに戻り、予測内の `is_correct` だけを無条件に信頼しない。raw logitsがなければ確率から生logitsを捏造しない。必要な推論だけ一度行う。

### 2.2 smokeの報告差分

M3.4報告では `smoke-11` は `hot` で誤答。M3.5報告では `cold` で「正解維持」と記述されている。同じ入力なら維持ではなく回復である。

5epと10epの保存個票をIDで対応させ、12行の正解・予測・正誤・遷移を出す。`smoke-04`と`smoke-11`の2回復、`smoke-08`の1退行なら、8+2-1=9と整合する。これは照合前の仮説なので、結果に合わせて記録を訂正する。違う入力・precision・重みを参照していた場合は原因と影響を明記する。

transfer32問も、共通正解・回復・退行・共通誤答を記録する。両者25/32であっても同じ25問への正解とは限らない。誤答時のpmax、正解確率、NLLの大きい数例を残す。再学習はしない。

以上に致命的な不整合がなければ先へ進む。報告文の誤記だけを理由に別の実験フェーズを増やさない。

## 3. 校正する用途を先に固定する

**今回の適用対象は、既存のgoal_followingとexplicit_ruleに近い、情報が本文に明示された合成判断問題。** 候補数は実際にデータで検証する2～3個を基本とし、APIが許す2～16個全体を校正したとは言わない。

NLI、問い合わせ、偽指示、自然な実業務文章全般を今回の校正範囲へ自動的に含めない。既存transferには複数の変形・別タスクが混在するため、単純に「すべて完全な分布外」と断定せず、校正対象外の診断集合として扱う。

**旧transferのNLLを下げることを、今回のT選択の目的にしない。** 合成ルールで選んだTがtransferでも有効かは、別の診断結果として報告する。転用問題の確率を一律に信頼できるようにする万能な処理とは考えない。[R1][R2]

## 4. 新規データ：まず200件＋200件

| 集合 | 目安 | 用途 |
|---|---:|---|
| calibration | 200問 / 100組 | Tの推定のみ |
| fresh_eval | 別の200問 / 100組 | 固定したTとT=1の比較 |
| 既存eval_v2 / smoke / transfer | 変更しない | 既知の診断。校正に流用しない |

これらは実験予算であり、実用精度を保証するサンプル数ではない。新規の学習データは作らない。

既存の生成器と正解検証を小さく再利用する。calibrationとfresh_evalのタスク・表現族・候補数・境界状態の配分は、モデルを実行する**前**に固定し、同じ対象分布から作る。例として各集合をGF100問・ER100問にし、ER内の複合・比較・境界を既存配分に合わせる。正解が同じペアと異なるペアを無理に同数へそろえず、実際の内訳を記録する。

### 4.1 重複と意味状態の分離

- 学習・dev・既知評価と新規2集合の間で、候補順に依存しないモデル入力指紋を比較する。本文・質問・候補textを使い、targetやIDで別物にしない。
- IDやseedを変えただけで新問とみなさない。生成元の意味状態も比較する。HP/閾値/アイテム、測定値/閾値、在庫/受注、候補と属性値の対応・質問条件などを使う。
- 同一シナリオの言い換え、2質問、候補順変更、翻訳は同じgroupとして分割する。
- 同じ論理形式を使うこと自体は禁止しない。例えばAND条件の新しい数値状態は対象分布内の新問になり得る。「同じルール形式」と「同じ元シナリオの言い換え」を区別する。
- 候補名だけを変えた同一の数値配置も、生成元で同一と分かるなら別splitへ配らない。汎用的な意味重複検出AIを新設する必要はない。
- 既存の誤った容量ラベル、二属性の順位固定、actual=threshold固定を再導入しない。最大・最小、等号、AND/ORについて、生成器と独立した少数の期待例を実処理に通す。

拒否サンプリングが無限に続く実装は避ける。既定の範囲内で新規状態が足りない場合は、無断で未知の数値範囲へ広げたり重複を許したりしない。配分を維持して件数を小さくできる場合は、モデル実行前に実数と制約を記録する。十分な分離を確保できなければ校正推定を保留する。

### 4.2 データの固定

入力・候補・正解の少数目視確認と正解プログラム検証後、データ・生成コード・構成のhashをmanifestに保存する。モデルを見て解ける問題だけを採用しない。プログラム検証を人間の独立確認と偽称しない。

fresh_evalの結果を見た後は、T、重み、問題配分、入力整形を調整しない。不正ラベルを発見した場合は採用判定を止め、元データを残して理由を記録する。成績を上げるための再生成はしない。

## 5. 校正は既存実装を再利用し、変数一つだけ

重み・tokenizer・入力整形・推論precisionを固定し、各集合の全候補logitsを一度取得して保存する。T比較のためにGPU推論を何度も回さない。

```text
log_p = log_softmax(real_candidate_logits / T)
NLL = mean(-log_p[correct_index])
p = exp(log_p)
T > 0
```

生logitsから安定したlog_softmaxで計算し、表示用に丸めた確率は使わない。GPUのforwardが混合精度でも後処理は最低FP32、CPUの小さな最適化はFP64を使ってよい。[R3]

- 初期T=1。calibrationのNLLだけで、log(T)等の変数一つを最適化する。
- 範囲は事前に `0.05 <= T <= 20` とし、最大100回程度の目的関数評価を上限とする。これは運用上の制約であって、最適温度の既知範囲ではない。
- 既存のPyTorch/NumPy/SciPyで足りる。新しい校正ライブラリや複数方式探索を加えない。
- T=1を比較候補に含め、最適化失敗、非有限値、境界到達、校正全問正解による過小Tへの偏りを記録する。無理にTを1～2へ収めない。境界なら今回の採用は保留してよい。
- 本物の候補だけで確率と損失を計算する。候補数違いのpaddingは除外する。
- 生logitsのargmaxをbestに使う。正の単一温度で順位・正答数・ペア正答数は変わらない。例外が出たら丸め、tie、mask、Tの二重適用、異なる入力や重みの混入を確認する。
- 外部の選択肢IDに予測内容を学習させたり、正解計算を推論へ混ぜたりしない。

**Tは全体で一つだけ。** タスク別T、NLIだけ別モデル、入力による温度切替、confidenceの新しい定義、確率を手で0.99以下へ丸める処理は追加しない。

## 6. 新規評価と転用診断を混ぜない

Tを確定してからfresh_evalを評価する。T=1と同じ保存logitsを使い、最低限以下を出す。

- 正解数/全問数、group数。全ペア・正解が異なるペア・正解が同じペア、および系統別の両問正解。
- NLL、`mean(sum_i((p_i-y_i)^2))`としてのBrier。2択/3択別も小さく分ける。
- 既存の確信度binごとの件数・平均pmax・正答率。binを成績に合わせて変更しない。空binを0%と書かない。
- 誤答のpmax・正解確率・NLL。高確信の誤答を省略しない。
- 固定した診断閾値0.8/0.9/0.95で、仮の採用件数・誤答件数・coverage・risk。採用0件のriskはnull。

閾値比較は観察用のみ。fresh_evalで最も都合のよい閾値を選んで自動採用を有効化しない。ペアを独立な2試行とみなし実用誤り率を保証しない。高度な統計基盤や有意差判定の自動化は作らない。

既存eval_v2・smoke・transferにも、**同じTをそのまま適用**してT=1と比べる。保存logitsがないものだけ再推論する。transferに合わせたTの再探索やデータ混合をしない。対象内と対象外を合算したスコアだけで効果を説明しない。

### 6.1 採否は、再探索のきっかけにしない

今回の保守的な実験用採用基準として、数値的に正常かつ探索境界ではなく、fresh_evalでNLLが低下しBrierも悪化しない場合に、**合成ルール用途限定の校正候補**として残す。数値誤差以内の同等はそのまま報告する。これは統計的な保証ではなく、あらかじめ決めた採否ルールである。

片方だけ改善、ほぼ同じ、不安定、成績悪化の場合も全結果を残し、採用を保留してT=1の実験モードを維持してよい。fresh_evalを見て探索範囲や温度を修正しない。NLLを下げるための再学習も行わない。

transferで改善しなければ、そのまま校正の限界として記録する。対象内で改善しても、転用全般を解決したとは言わない。正しい候補を選べない問題は温度だけでは修正できない。

## 7. 成果物・既存APIとの結合は最小限

`calibration.json`の既存仕様を使い、実ファイルhashにより10epの重み・config・tokenizer・入力整形・precision・入力上限へ結び付ける。calibrationデータhash、T、探索状態、適用範囲も記録する。5epモデルや誤ラベル旧モデルの校正ファイルを流用しない。

不一致は起動時に拒否する。校正なしは明示的に `calibration.status=none`、適用した場合は `applied` と実artifact_idを返す。`applied`は温度適用の事実であり、その入力の正答確率が保証されたという意味ではない。

今回、新しいHTTP機能を作らない。既存のAPI/CLIで10epの明示指定と校正ファイルの適用が可能なら、正常1～2問について対応だけ確認する。全件 `decision.status=review` を維持し、既存の常駐サーバーを勝手に切り替えない。接続確認のための起動はloopback限定、1 worker、reloadなし。自分が起動したテストプロセスだけ終了する。

校正の採用を保留しても、現モデルを未校正・review固定で実験できる状態を残せばよい。UI、外部公開、DB、モデルレジストリは不要。

## 8. テストと完了報告

既存テストを再利用する。今回変更した箇所に不足がある場合だけ、次の責務を少数ケースで確認する。

- T=1の整合、正で有限なT、実候補のmask、非同率bestの不変性。
- 校正のモデル不一致拒否、未校正モード、Tを一度だけ適用すること。
- 新規splitでの入力・元シナリオ分離と、実際の正解処理の期待値。

通常pytestでGPUやモデルダウンロードを要求しない。モデルの正答率をユニットテストの成功条件にしない。カバレッジ目標や件数目標を作らない。生成器を呼ばず定数同士だけを比較するテストで代用しない。

保存先の例：`runs/m3_6_calibration/`。既存の保存形式を優先する。

最終報告は、受領した成績・今回実測した成績・仮説を分け、以下を含める。

1. 10ep対象の実hashと、M3.5個票照合。smoke-11の維持/回復の訂正。
2. calibration / fresh_evalの実問数・group数・分布・正解検証・意味状態分離。
3. T、探索状態、T=1との差、fresh_evalのNLL/Brier/確信度帯/切替ペア。
4. transfer・smokeの別集計と、対象範囲の限界。悪化した指標も残す。
5. 採用/保留と理由。review固定・API対応確認の実施範囲。
6. 実行したコマンド、テスト、未実施項目。次の課題を一つだけ。

**温度を採用できなくても、この一回の検証で有効な範囲と限界が分かれば完了。** M3.5で成立した通常CEの学習を無条件にやり直さない。

### レビューZIP

新しいパッケージング基盤は作らず、既存ZIP作成処理を再利用する。M3.6の関連コード・データ・logits・個票・集計・manifest・校正候補・採否メモ・最新STATUSを収録する。M3.5の設定、train_summary、5ep/10epのtrain-dev診断、比較個票も、今回の照合に必要な小ファイルを含める。参照元が欠けたら欠落を記録し、旧版のファイルを最新版と偽らない。

重み・optimizer state・.venv・HFキャッシュ・秘密情報・個人データ・環境変数ダンプは除外。作成後に内容一覧・不足・サイズ・hashを確認する。アップロードはユーザーが行う操作と区別する。

## 9. 出所と技術背景

[F1] 今回添付「貼り付けたマークダウン（1）(7).md」、M3.5報告§1〜7。成績・ハッシュ・提案の出所。作成者による再実行結果ではない。

[F2] 前回添付「貼り付けたマークダウン（1）(6).md」、M3.4報告§5。smoke-11のhot誤答という記録。M3.5報告との差分はローカル個票で確認する。

[R1] Guo et al., On Calibration of Modern Neural Networks, ICML 2017。単一温度による事後校正の背景。ERABIでの有効性を保証しない。
```text
https://proceedings.mlr.press/v70/guo17a.html
```

[R2] Ovadia et al., Can You Trust Your Model's Uncertainty? Evaluating Predictive Uncertainty Under Dataset Shift, NeurIPS 2019。分布変化下で事後校正が十分でない場合の研究。今回のERABIを直接評価したものではない。
```text
https://arxiv.org/abs/1906.02530
```

[R3] PyTorch公式 log_softmax。数値的に安定した計算とdtype指定。参照は2.14へリダイレクトされたが、ローカル導入版を無断更新しない。
```text
https://docs.pytorch.org/docs/stable/generated/torch.nn.functional.log_softmax.html
```

外部資料確認日：2026-09-18。データ数、探索範囲、採否基準、実装範囲はERABI向けの今回の設計判断。
