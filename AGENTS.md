# ERABI project instructions

## 目的と正本

ERABIは、context・question・可変choicesから全候補の確率分布を返すローカル判断エンジン。Jevの公式版・内部再現版ではない。
仕様の正本は `docs/ERABI_DESIGN.md`。利用方法は `README.md`、実測と未完了項目は `STATUS.md` を参照する。過去の段階別指示書はGit履歴に保存されている。

## 作業範囲

現在の作業範囲はユーザーの指示と実測に従う。計画だけで終了せず、権限内で小さな実装と実行を進める。
既存ファイル、未コミットの変更、他のプロジェクトの環境を最初に確認し、上書きしない。仮想環境を分離し、動いているGPUジョブを勝手に停止しない。GPU1基で開始する。

## 最小構成

Pythonの薄い実装から始める。既存GLiClassを先に測り、日本語ModernBERTは不足が確認された場合だけ検討する。選択肢は初期2～16、全入力512 tokens。無言のtruncationは禁止。
UI、DB、RAG、Redis、Ray、Celery、Kubernetes、分散学習、独自plugin、汎用DI、オンライン学習を初期に導入しない。第二の利用先ができる前の共通基盤を作らない。
依存は必要なものだけ。LoRA、量子化、ONNX等は測定上の必要性が出てから。空クラスや未使用の将来機能を先に大量作成しない。

## 数値とデータの不変条件

候補IDと順序を保持し、全候補のlogitsを取得する。top1以外へ0を補って全分布を装わない。Sigmoid値の再正規化や二重Softmaxは禁止。
padding候補はSoftmax・損失から除外し、0 * -infによるNaNを防ぐ。非有限値を成功として返さない。
候補順序を変えたら正解・教師分布も同じpermutationで変更する。学習と推論の入力整形を共通化する。
group単位でtrain/dev/calibration/final_testを分ける。例示用データを人手確認済みgoldと呼ばない。教師確率は正解確率の保証ではない。
校正はモデル選択後に独立データで行う。校正ファイルを対象weightsと結び付ける。最大確率や温度適用を正解保証と呼ばない。自動処理はreview既定。

## テストを過剰にしない

CPU・ネット不要の小さなテストで、入力契約、ID対応、mask、損失、split、保存、校正の重要箇所を確認する。該当機能が未実装なら、その将来テスト群は作らない。
100%カバレッジやテスト件数を目標にしない。trivial getter、外部ライブラリの内部、全メソッドのmockテストは不要。
GPUスモーク・小規模過学習・モデル品質評価は通常のpytestから分離し、明示的に実行する。学習モデルに固定の確率値を要求しない。候補順序変更による意味的な安定性は評価で測り、完全不変のunit testにしない。

## 権限と安全

課金API・外部への教材送信は、提供元・モデル・予算・送信可能データが指定された範囲内のみ実行する。公開モデルや依存の取得は、対象・容量・保存先を確認し、利用可能な権限の範囲で行う。
管理者権限、ドライバー変更、既存環境の破壊的変更、リモート公開、外部ポート公開は無断で行わない。信頼できないモデルコードやpickleを無条件で実行しない。
APIキーや個人情報をGit・ログ・教材へ混ぜない。推論APIはlocalhost。既存モデルやrunを勝手に削除しない。

## 報告と中断

実行したコマンド、実測結果、未実行項目を区別する。GPUやデータが不足してもfakeの成績を作らない。仕様と実装がずれたら理由を記録する。
節目でSTATUS.mdを短く更新し、完了・未完了・詰まり・次の一手を残す。長い説明を何度も繰り返さず、変更に対応する必要な検証を実施する。
