# ERABI — M3.7：校正成果物の生成・照合・API適用を仕上げる

作成日：2026-09-18  
対象：Codex / Antigravityで既存erabi-localを実装する担当者  
前提：M3.6のZIPと保存logitsのレビュー結果を受けた追加指示。技術契約は既存AGENTS.md・設計書を維持する。

## 0. 今回の結論と範囲

**数値面の校正結果は支持された。再学習や温度探索をやり直さず、成果物の生成と読み込みの不一致を修正する。**

最初に `AGENTS.md`、現在の `STATUS.md`、`ERABI_M3_6_REVIEW.md`、該当する実装を読む。計画を示すだけで止めず、以下の小さな修正・CPU確認・実機接続・記録まで実施する。ツール側の承認や既存環境の保護は守る。

固定するもの：

| 項目 | 方針 |
|---|---|
| 重み | `runs/m3_5_ce10/trained/checkpoint` のW_v2_ce10を保持 |
| 対象重みhash | `6c9cac0610798cb031cbc6513b3e7483d4e130ade7bf9da07818e23c96d39b51`。実ファイルで再確認 |
| データ | M3.6 calibration/fresh_eval各200問を変更しない |
| 温度 | 保存された推定値 `3.109686582278409`。現在の配信用丸め値は3.1097 |
| API | localhost、1 worker、全件reviewを維持 |
| 変更しない機能 | 学習方法、データ量、モデル構造、入力問題の意味 |

**今回は行わない：** 再学習、ペア損失、NLI/ルーティング等の混合学習、新しい評価問題の生成、温度の再探索、モデル交換、量子化、外部公開、自動accept、UI、DB、registry、汎用キャッシュ基盤、全体リファクタリング。

旧成果物は上書きしない。新しい小さな移行成果物・接続結果を `runs/m3_7_calibration_handoff/` 等に保存する。M3.6のレビュー結果は削除せず、必要な訂正を追記する。

## 1. 既に支持された結果を、再実験で取り直さない

保存された644件のlogitsの独立計算で以下が確認された。

- fresh_eval：188/200、NLL 0.348948→0.167539、Brier 0.117950→0.095721。
- calibrationだけを使う温度推定：約3.1097。fresh_evalを温度選びに使っていない。
- 新規400件の既知合成文法のラベルに不一致なし。
- 新規データと既存の同型データに、独立チェックで完全一致/抽出可能な意味状態の重複は見つからなかった。

ただし実モデルや重みファイルはレビュー環境にはなく、logitsの生成元を重みバイナリから再確認したものではない。修正はこの不足を適切に記録し、今後の再利用時に出所を検証できるようにするためである。

## 2. 状態名を既存契約に統一する（最優先）

### 2.1 現象

作成スクリプトは採用時に `applied_scoped` を書き、APIは `applied` の場合だけTを適用する。同梱calibration.jsonは手編集後の `applied` である。現スクリプトで再生成したままのファイルでは、既定設定でT=1/noneへ戻る。

### 2.2 修正

runtimeの状態は既存契約を保つ。

```json
{
  "status": "applied",
  "adoption": {"decision": "ACCEPT_SCOPED"},
  "contract": {"scope": "今回検証した合成問題分布の説明"}
}
```

上記はフィールド分離の部分例であり、既存の必須情報を削らない。`applied_scoped` を新しいruntime状態として増やすのではなく、限定範囲はadoption/scopeに記録する。

- 校正の採用判断と、runtimeで温度を適用する状態を分ける。
- 未知状態は理由付きエラー。校正ファイルを明示しているのに、未知状態を理由に黙ってT=1で続行しない。
- 校正を使わない起動は、校正指定なしの明示経路として残す。
- 今回の接続確認は `--require-calibration` を指定する。
- 温度は最適化せず、既存推定値をそのまま使う。内部値を保存するか既存3.1097を維持するかを明記し、CLI/API/評価で同じ値を使う。丸め方を変えるだけなら既存logitsで差を確認し、新しい最適化とは呼ばない。

artifactを書き出す既存部分を小さな関数として呼べるようにする程度で十分。生成スクリプト全体やGPU推論をCPUテストから起動しない。

## 3. 校正付き起動は、モデルと入力条件が照合できたときだけ許可する

### 3.1 ローカルcheckpoint照合

`load_and_verify_calibration` は、対象がディレクトリでないとhash検査を飛ばしてappliedを返す。これを修正する。

- 今回は既存のローカルcheckpointディレクトリを必須にする。
- 存在しないパスや、未解決の公開モデル名は、校正適用前に拒否する。リモート解決機構を新設しない。
- 必須の重み・config・tokenizer情報を実ファイルで照合する。任意の1ファイルだけのhashが合えば完了、にはしない。
- 別モデルのhash、必須ファイル欠落、空manifestはエラーにする。
- hashは起動時に確認し、リクエストごとに巨大ファイルを読み直さない。
- 共有hash helperのimportのためにモデルライブラリをロードする必要はない。必要なら数行のhelperを軽い既存モジュールへ移す。汎用utility層を大量に作らない。

### 3.2 formatter・precision等の契約

現実装はartifactの `contract` を適用条件と比較していない。

最低限、実際に使う入力整形の版、schema、整形後入力上限、forwardのdtype/AMP条件、確率後処理の条件を記録・照合する。モデルのraw logitsをFP32へ変換しているだけで、forward全体がFP32だったと断言しない。

文字列形式は既存に合わせて簡潔にする。サーバーと作成側が共有する小さな定数や辞書で足りる。モデル/formatter/precisionの不一致は起動前に拒否する。

現在のartifactにない過去情報を、後付けして生成当時に検証済みだったかのように記録しない。現コード・設定・保存記録・確認用推論を照合して移行した事実と、元の生成記録を分ける。現時点で証拠不足ならlegacy扱いを明示し、検証済みの通常cacheとして無条件再利用しない。

## 4. 保存logitsの再利用条件を、小さなmanifestで明示する

現スクリプトは `raw_logits_*.jsonl` の存在だけで再利用し、現在のモデルhashを後からartifactへ書く。このままだと将来モデルやデータを変えた際に混同できる。

新しくlogitsを書く際、同じ処理で小さなsidecarを残す。

- 実際に使用したローカルモデル関連ファイルhash。
- 入力JSONLのhash、件数、入力整形の版、実行精度。
- logitsファイルのhashと生成日時。
- 候補順・ID・正解位置の対応がどの入力に結び付くか。

再利用時にはmanifestと現設定、入力/出力ファイルを照合する。ID欠落・重複・候補順の不一致・正解位置の不一致も拒否する。不一致なら理由を示して止め、勝手にcacheやデータを上書きしない。

**既存cacheの移行：** M3.6成果物は今回の数値レビューに使えるが、生成時sidecarが存在しない。履歴を偽造して埋めない。

1. 当時の固定重み・コード・データ・実行条件を保存記録から確認する。
2. 十分な出所を説明できるなら、元logits hashと移行時確認を別途記録して移行する。「生成時にsidecarあり」とは書かない。
3. 必要情報が足りず新しい適用契約を検証できない場合に限り、同じ重み・同じ400件を一度再推論して新規パスへ保存し、旧logitsと照合してよい。これは再学習・データ再生成・温度再探索ではない。差が大きい場合は原因を記録し、旧温度を検証済みとして自動適用しない。

数件の推論一致だけで全cacheの歴史的な出所を完全証明した、と書かない。今回モデル起源の証拠が限定的であっても、どこまで確認したかを記録すればよい。DB、モデルregistry、キャッシュサービス等は不要。

## 5. GFの意味状態キーだけを統一し、現データを再検査する

新規GFキーと既存読込キーの形が違い、同じ1組を保存・再読込しても自分自身に一致しない。

- ドメイン/属性単位と各候補の属性値の対応を保持したcanonical keyに統一する。
- 候補一覧や本文の順序を入れ替えても、同じ対応なら同じ状態と判定する。
- 属性1・属性2を別々にソートして対応関係を失わない。
- 表記の違いによる同義性を任意自然文で解く仕組みは不要。既存生成器が作る文法のみ対象にする。
- 解析対象外は未対応件数として記録し、未対応なのに「意味状態重複ゼロ」と総括しない。
- 現在のcalibration/fresh_eval/既存同型データを再点検する。データは書き換えず、再生成しない。

独立レビューでは実データの重複は見つからなかった。修正後に別の結果が出たら、対象IDと状態を保存して相違を調べる。自動的に別データを作って評価し直さない。

## 6. 検証は重要な責務だけ

新しい大規模テスト基盤、カバレッジ目標、テスト件数の目標を設けない。既存テストと重なるものは使い回す。

| 責務 | 最小の確認 |
|---|---|
| artifactの往復 | 実際の書き出し処理→ローダー→APIを通し、T・artifact ID・applied・reviewが維持される |
| 誤適用の拒否 | 存在しないcheckpoint、hash/契約不一致、未知statusを小さなダミーファイルで拒否 |
| cacheの結合 | データまたはモデル指定を変えると、古いlogitsの再利用を拒否 |
| 状態キーの往復 | 実際の生成関数の結果を保存・再読込すると同じキーに戻る |

通常のpytestはCPUで、モデルダウンロードやGPU起動をしない。実モデルでの意味的な正答率をユニットテストの合否条件にしない。

校正の既存NLL最適化処理は今回は変更しない。無関係な古いテストや依存警告の全面整理も不要。

## 7. 実機接続は、既存APIで2～3問

手で編集したJSONではなく、修正後の書き出し処理が作った新artifactを使う。サーバーは127.0.0.1、1worker、reloadなし、require-calibrationを指定する。

1. 起動時に実モデル・tokenizer等のhashと契約の一致を確認。
2. `/health` が期待したT、完全なartifact ID、applied、review_defaultを返す。
3. 既存データから2～3問を選び、同じモデル・入力・TのCLI/推論関数とHTTPの全候補分布が許容差内で一致する。
4. Tを二重に適用していないことを、同じraw logitsのsoftmax(z/T)と照合する。正解候補を返すかだけでは温度適用を検証できない。
5. 全件reviewを確認。known high-confidence errorを理由なく隠さない。
6. 自分で起動したサーバーだけを停止する。他のPythonやGPUジョブを止めない。

`HF_HUB_OFFLINE=1`とローカル読込は、プロセス全体の通信遮断試験ではない。OSのネットワーク設定を変えず、実施した範囲を記録する。

## 8. 既存報告の補足は短く

- smoke維持は7件（回復2・退行1・継続誤答2）。最終正解9件は変わらない。
- 「探索境界外」は「探索範囲の内部・境界非到達」と訂正。
- 非ペアのsmokeに無意味なpair_statsを返す部分、4択を3択欄へ集計する部分を必要最小限で修正。保存logitsの再集計だけでよい。
- scopeは「本文にルールが書かれている任意問題」ではなく、検証した生成分布・語彙・条件形式・2～3択まで。4～16択はAPI契約上受理できても校正有効性は未検証。
- `smoke-06`は例外優先ルールを全て与えていても、校正後約99.65%でhealを誤選択する。自動acceptは有効にしない。
- fresh_evalの高信頼帯の平均一致は、小標本での観測。完全校正や未知用途の正答率保証と書かない。

## 9. 完了条件と保存物

今回の完了条件は、**同じ重み・同じ温度で、生成したartifactが手編集なしに検証されてAPIへ適用されること**。別タスクの精度向上は求めない。

既存構成を使い、例えば以下を残す。

```text
runs/m3_7_calibration_handoff/
  calibration.json           # 新ID、旧Tの再利用と移行根拠を記録
  migration_notes.md         # 元artifact/hash、確認範囲、不足した出所情報
  verification.json         # ローカル実機とCPU境界の成否
  cache_manifest.json       # 必要な場合。実際に生成/移行した記録
  semantic_check.json       # 比較対象・解析件数・重複件数
  notes.md                  # 実行コマンド、修正、未実行
  review_bundle.zip
```

ファイルをこの通り増やすこと自体は目的ではない。既存のJSONやnotesへまとめてもよい。変えたsource、最小テスト、artifact、実行記録、必要な小さい個票をレビューZIPに含める。重み、.venv、秘密情報、環境変数ダンプは含めない。

README/STATUSへ、実際に通った起動コマンド、期待する温度/状態、現在の限界を短く追記する。出力を見ずに「すべて完成」と先に書かない。現在の数値レビューが支持したことと、今回のruntime確認が支持したことを分ける。

ここまで通れば、M3の合成ルール実験版は一度保全して区切る。次の能力拡張は別フェーズで一用途だけを対象に比較する。今回ついでにNLI・ルーティング・否定・例外・プロンプト防御を全部混ぜない。

## 10. 出所

この指示書の不具合は、ユーザーが提供したM3.6 ZIP内の実装と、`ERABI_M3_6_REVIEW` のCPU再現・保存logits再計算に基づく。件数・実施順・移行方式はERABI向けの設計判断であり、外部ライブラリが必須と定めた手順ではない。
